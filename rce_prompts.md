# Remote Code Execution Bug Bounty Skills

A curated, extensive collection of 161 advanced skills for Command Injection and RCE bug bounty hunting. Built for the Bug Bounty System Harness.

### 1. Remote Code Execution - OS Command Injection via Filenames targeting Rust (Actix)

**Target Area**: Internal Logic / Rust (Actix) architecture
**Description**: An extensive operational guide for identifying and exploiting OS Command Injection via Filenames within a Authenticated endpoint acting as an administrator. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Rust (Actix) parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Authenticated endpoint acting as an administrator. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on OS Command Injection via Filenames. Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 5 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Full system compromise leading to a reverse shell.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 2. Remote Code Execution - OS Command Injection via Filenames targeting Rust (Actix)

**Target Area**: Internal Logic / Rust (Actix) architecture
**Description**: An extensive operational guide for identifying and exploiting OS Command Injection via Filenames within a Beta feature accessible via feature flags. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Rust (Actix) parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Beta feature accessible via feature flags. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on OS Command Injection via Filenames. Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 6 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Deployment of cryptominers across the backend fleet.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 3. Remote Code Execution - Insecure Deserialization (PHP) targeting ASP.NET Core

**Target Area**: Internal Logic / ASP.NET Core architecture
**Description**: An extensive operational guide for identifying and exploiting Insecure Deserialization (PHP) within a Beta feature accessible via feature flags. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how ASP.NET Core parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Beta feature accessible via feature flags. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on Insecure Deserialization (PHP). Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 6 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Lateral movement to internal databases.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 4. Remote Code Execution - Command Injection via eval() targeting GraphQL (Apollo)

**Target Area**: Internal Logic / GraphQL (Apollo) architecture
**Description**: An extensive operational guide for identifying and exploiting Command Injection via eval() within a Internal microservice communicating via proxy. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how GraphQL (Apollo) parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Internal microservice communicating via proxy. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on Command Injection via eval(). Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 7 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Lateral movement to internal databases.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 5. Remote Code Execution - Insecure Deserialization (PHP) targeting Firebase

**Target Area**: Internal Logic / Firebase architecture
**Description**: An extensive operational guide for identifying and exploiting Insecure Deserialization (PHP) within a Unauthenticated endpoint exposed to the public. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Firebase parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Unauthenticated endpoint exposed to the public. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on Insecure Deserialization (PHP). Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 3 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Deployment of cryptominers across the backend fleet.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 6. Remote Code Execution - Insecure Deserialization (PHP) targeting Symfony (PHP)

**Target Area**: Internal Logic / Symfony (PHP) architecture
**Description**: An extensive operational guide for identifying and exploiting Insecure Deserialization (PHP) within a Authenticated endpoint acting as an administrator. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Symfony (PHP) parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Authenticated endpoint acting as an administrator. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on Insecure Deserialization (PHP). Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 4 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Full system compromise leading to a reverse shell.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 7. Remote Code Execution - RCE via YAML Parsing Flaws targeting Symfony (PHP)

**Target Area**: Internal Logic / Symfony (PHP) architecture
**Description**: An extensive operational guide for identifying and exploiting RCE via YAML Parsing Flaws within a Mobile application backend API. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Symfony (PHP) parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Mobile application backend API. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on RCE via YAML Parsing Flaws. Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 5 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Lateral movement to internal databases.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 8. Remote Code Execution - Server-Side Template Injection (Twig) targeting Rust (Actix)

**Target Area**: Internal Logic / Rust (Actix) architecture
**Description**: An extensive operational guide for identifying and exploiting Server-Side Template Injection (Twig) within a Internal microservice communicating via proxy. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Rust (Actix) parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Internal microservice communicating via proxy. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on Server-Side Template Injection (Twig). Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 5 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Deployment of cryptominers across the backend fleet.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 9. Remote Code Execution - Server-Side Template Injection (Jinja2) targeting ASP.NET Core

**Target Area**: Internal Logic / ASP.NET Core architecture
**Description**: An extensive operational guide for identifying and exploiting Server-Side Template Injection (Jinja2) within a Unauthenticated endpoint exposed to the public. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how ASP.NET Core parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Unauthenticated endpoint exposed to the public. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on Server-Side Template Injection (Jinja2). Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 7 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Deployment of cryptominers across the backend fleet.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 10. Remote Code Execution - Remote Code Execution via File Upload targeting Supabase

**Target Area**: Internal Logic / Supabase architecture
**Description**: An extensive operational guide for identifying and exploiting Remote Code Execution via File Upload within a Mobile application backend API. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Supabase parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Mobile application backend API. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on Remote Code Execution via File Upload. Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 5 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Exfiltration of all environment variables and secrets.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 11. Remote Code Execution - Insecure Deserialization (Java) targeting FastAPI

**Target Area**: Internal Logic / FastAPI architecture
**Description**: An extensive operational guide for identifying and exploiting Insecure Deserialization (Java) within a Authenticated endpoint acting as an administrator. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how FastAPI parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Authenticated endpoint acting as an administrator. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on Insecure Deserialization (Java). Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 7 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Full system compromise leading to a reverse shell.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 12. Remote Code Execution - Server-Side Template Injection (Twig) targeting Azure API Management

**Target Area**: Internal Logic / Azure API Management architecture
**Description**: An extensive operational guide for identifying and exploiting Server-Side Template Injection (Twig) within a Authenticated endpoint acting as an administrator. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Azure API Management parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Authenticated endpoint acting as an administrator. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on Server-Side Template Injection (Twig). Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 4 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Exfiltration of all environment variables and secrets.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 13. Remote Code Execution - RCE via YAML Parsing Flaws targeting Flask (Python)

**Target Area**: Internal Logic / Flask (Python) architecture
**Description**: An extensive operational guide for identifying and exploiting RCE via YAML Parsing Flaws within a Internal microservice communicating via proxy. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Flask (Python) parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Internal microservice communicating via proxy. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on RCE via YAML Parsing Flaws. Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 6 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Lateral movement to internal databases.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 14. Remote Code Execution - RCE via YAML Parsing Flaws targeting FastAPI

**Target Area**: Internal Logic / FastAPI architecture
**Description**: An extensive operational guide for identifying and exploiting RCE via YAML Parsing Flaws within a Third-party integration webhook. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how FastAPI parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Third-party integration webhook. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on RCE via YAML Parsing Flaws. Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 3 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Deployment of cryptominers across the backend fleet.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 15. Remote Code Execution - Command Injection via eval() targeting Symfony (PHP)

**Target Area**: Internal Logic / Symfony (PHP) architecture
**Description**: An extensive operational guide for identifying and exploiting Command Injection via eval() within a Multi-tenant application environment. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Symfony (PHP) parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Multi-tenant application environment. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on Command Injection via eval(). Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 4 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Exfiltration of all environment variables and secrets.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 16. Remote Code Execution - Insecure Deserialization (Python Pickle) targeting GraphQL (Relay)

**Target Area**: Internal Logic / GraphQL (Relay) architecture
**Description**: An extensive operational guide for identifying and exploiting Insecure Deserialization (Python Pickle) within a Multi-tenant application environment. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how GraphQL (Relay) parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Multi-tenant application environment. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on Insecure Deserialization (Python Pickle). Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 3 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Lateral movement to internal databases.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 17. Remote Code Execution - Insecure Deserialization (Java) targeting Spring Boot (Java)

**Target Area**: Internal Logic / Spring Boot (Java) architecture
**Description**: An extensive operational guide for identifying and exploiting Insecure Deserialization (Java) within a Unauthenticated endpoint exposed to the public. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Spring Boot (Java) parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Unauthenticated endpoint exposed to the public. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on Insecure Deserialization (Java). Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 6 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Full system compromise leading to a reverse shell.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 18. Remote Code Execution - Server-Side Template Injection (Twig) targeting Laravel (PHP)

**Target Area**: Internal Logic / Laravel (PHP) architecture
**Description**: An extensive operational guide for identifying and exploiting Server-Side Template Injection (Twig) within a Mobile application backend API. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Laravel (PHP) parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Mobile application backend API. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on Server-Side Template Injection (Twig). Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 5 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Exfiltration of all environment variables and secrets.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 19. Remote Code Execution - RCE via YAML Parsing Flaws targeting Node.js/Express

**Target Area**: Internal Logic / Node.js/Express architecture
**Description**: An extensive operational guide for identifying and exploiting RCE via YAML Parsing Flaws within a Authenticated endpoint acting as an administrator. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Node.js/Express parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Authenticated endpoint acting as an administrator. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on RCE via YAML Parsing Flaws. Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 7 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Exfiltration of all environment variables and secrets.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 20. Remote Code Execution - Server-Side Template Injection (Twig) targeting FastAPI

**Target Area**: Internal Logic / FastAPI architecture
**Description**: An extensive operational guide for identifying and exploiting Server-Side Template Injection (Twig) within a Mobile application backend API. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how FastAPI parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Mobile application backend API. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on Server-Side Template Injection (Twig). Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 7 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Deployment of cryptominers across the backend fleet.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 21. Remote Code Execution - Insecure Deserialization (Java) targeting Firebase

**Target Area**: Internal Logic / Firebase architecture
**Description**: An extensive operational guide for identifying and exploiting Insecure Deserialization (Java) within a Third-party integration webhook. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Firebase parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Third-party integration webhook. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on Insecure Deserialization (Java). Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 6 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Deployment of cryptominers across the backend fleet.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 22. Remote Code Execution - Remote Code Execution via File Upload targeting Supabase

**Target Area**: Internal Logic / Supabase architecture
**Description**: An extensive operational guide for identifying and exploiting Remote Code Execution via File Upload within a Authenticated endpoint acting as an administrator. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Supabase parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Authenticated endpoint acting as an administrator. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on Remote Code Execution via File Upload. Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 7 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Exfiltration of all environment variables and secrets.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 23. Remote Code Execution - Remote Code Execution via File Upload targeting Apigee

**Target Area**: Internal Logic / Apigee architecture
**Description**: An extensive operational guide for identifying and exploiting Remote Code Execution via File Upload within a Mobile application backend API. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Apigee parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Mobile application backend API. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on Remote Code Execution via File Upload. Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 7 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Exfiltration of all environment variables and secrets.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 24. Remote Code Execution - Remote Code Execution via File Upload targeting Flask (Python)

**Target Area**: Internal Logic / Flask (Python) architecture
**Description**: An extensive operational guide for identifying and exploiting Remote Code Execution via File Upload within a Legacy API endpoint slated for deprecation. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Flask (Python) parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Legacy API endpoint slated for deprecation. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on Remote Code Execution via File Upload. Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 6 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Full system compromise leading to a reverse shell.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 25. Remote Code Execution - Insecure Deserialization (Python Pickle) targeting Flask (Python)

**Target Area**: Internal Logic / Flask (Python) architecture
**Description**: An extensive operational guide for identifying and exploiting Insecure Deserialization (Python Pickle) within a Third-party integration webhook. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Flask (Python) parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Third-party integration webhook. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on Insecure Deserialization (Python Pickle). Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 3 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Exfiltration of all environment variables and secrets.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 26. Remote Code Execution - Server-Side Template Injection (Twig) targeting GraphQL (Relay)

**Target Area**: Internal Logic / GraphQL (Relay) architecture
**Description**: An extensive operational guide for identifying and exploiting Server-Side Template Injection (Twig) within a Third-party integration webhook. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how GraphQL (Relay) parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Third-party integration webhook. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on Server-Side Template Injection (Twig). Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 4 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Full system compromise leading to a reverse shell.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 27. Remote Code Execution - Insecure Deserialization (Java) targeting Flask (Python)

**Target Area**: Internal Logic / Flask (Python) architecture
**Description**: An extensive operational guide for identifying and exploiting Insecure Deserialization (Java) within a Beta feature accessible via feature flags. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Flask (Python) parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Beta feature accessible via feature flags. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on Insecure Deserialization (Java). Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 7 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Exfiltration of all environment variables and secrets.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 28. Remote Code Execution - Command Injection via shell_exec targeting Flask (Python)

**Target Area**: Internal Logic / Flask (Python) architecture
**Description**: An extensive operational guide for identifying and exploiting Command Injection via shell_exec within a Authenticated endpoint acting as an administrator. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Flask (Python) parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Authenticated endpoint acting as an administrator. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on Command Injection via shell_exec. Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 4 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Deployment of cryptominers across the backend fleet.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 29. Remote Code Execution - Insecure Deserialization (Python Pickle) targeting Go (Fiber)

**Target Area**: Internal Logic / Go (Fiber) architecture
**Description**: An extensive operational guide for identifying and exploiting Insecure Deserialization (Python Pickle) within a Unauthenticated endpoint exposed to the public. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Go (Fiber) parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Unauthenticated endpoint exposed to the public. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on Insecure Deserialization (Python Pickle). Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 3 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Lateral movement to internal databases.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 30. Remote Code Execution - Command Injection via shell_exec targeting Laravel (PHP)

**Target Area**: Internal Logic / Laravel (PHP) architecture
**Description**: An extensive operational guide for identifying and exploiting Command Injection via shell_exec within a Unauthenticated endpoint exposed to the public. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Laravel (PHP) parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Unauthenticated endpoint exposed to the public. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on Command Injection via shell_exec. Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 4 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Deployment of cryptominers across the backend fleet.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 31. Remote Code Execution - OS Command Injection via Filenames targeting Laravel (PHP)

**Target Area**: Internal Logic / Laravel (PHP) architecture
**Description**: An extensive operational guide for identifying and exploiting OS Command Injection via Filenames within a Multi-tenant application environment. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Laravel (PHP) parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Multi-tenant application environment. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on OS Command Injection via Filenames. Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 4 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Full system compromise leading to a reverse shell.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 32. Remote Code Execution - Remote Code Execution via File Upload targeting Go (Fiber)

**Target Area**: Internal Logic / Go (Fiber) architecture
**Description**: An extensive operational guide for identifying and exploiting Remote Code Execution via File Upload within a Legacy API endpoint slated for deprecation. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Go (Fiber) parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Legacy API endpoint slated for deprecation. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on Remote Code Execution via File Upload. Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 5 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Lateral movement to internal databases.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 33. Remote Code Execution - Server-Side Template Injection (Jinja2) targeting FastAPI

**Target Area**: Internal Logic / FastAPI architecture
**Description**: An extensive operational guide for identifying and exploiting Server-Side Template Injection (Jinja2) within a Multi-tenant application environment. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how FastAPI parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Multi-tenant application environment. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on Server-Side Template Injection (Jinja2). Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 3 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Lateral movement to internal databases.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 34. Remote Code Execution - Remote Code Execution via File Upload targeting Flask (Python)

**Target Area**: Internal Logic / Flask (Python) architecture
**Description**: An extensive operational guide for identifying and exploiting Remote Code Execution via File Upload within a Authenticated endpoint acting as an administrator. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Flask (Python) parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Authenticated endpoint acting as an administrator. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on Remote Code Execution via File Upload. Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 4 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Full system compromise leading to a reverse shell.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 35. Remote Code Execution - Server-Side Template Injection (Twig) targeting Go (Gin)

**Target Area**: Internal Logic / Go (Gin) architecture
**Description**: An extensive operational guide for identifying and exploiting Server-Side Template Injection (Twig) within a Third-party integration webhook. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Go (Gin) parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Third-party integration webhook. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on Server-Side Template Injection (Twig). Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 4 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Lateral movement to internal databases.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 36. Remote Code Execution - Remote Code Execution via File Upload targeting FastAPI

**Target Area**: Internal Logic / FastAPI architecture
**Description**: An extensive operational guide for identifying and exploiting Remote Code Execution via File Upload within a Mobile application backend API. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how FastAPI parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Mobile application backend API. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on Remote Code Execution via File Upload. Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 6 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Exfiltration of all environment variables and secrets.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 37. Remote Code Execution - Command Injection via shell_exec targeting Firebase

**Target Area**: Internal Logic / Firebase architecture
**Description**: An extensive operational guide for identifying and exploiting Command Injection via shell_exec within a Unauthenticated endpoint exposed to the public. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Firebase parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Unauthenticated endpoint exposed to the public. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on Command Injection via shell_exec. Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 7 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Lateral movement to internal databases.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 38. Remote Code Execution - OS Command Injection via Filenames targeting Django (Python)

**Target Area**: Internal Logic / Django (Python) architecture
**Description**: An extensive operational guide for identifying and exploiting OS Command Injection via Filenames within a Third-party integration webhook. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Django (Python) parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Third-party integration webhook. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on OS Command Injection via Filenames. Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 3 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Full system compromise leading to a reverse shell.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 39. Remote Code Execution - Server-Side Template Injection (Twig) targeting Rust (Actix)

**Target Area**: Internal Logic / Rust (Actix) architecture
**Description**: An extensive operational guide for identifying and exploiting Server-Side Template Injection (Twig) within a Unauthenticated endpoint exposed to the public. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Rust (Actix) parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Unauthenticated endpoint exposed to the public. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on Server-Side Template Injection (Twig). Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 5 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Deployment of cryptominers across the backend fleet.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 40. Remote Code Execution - Server-Side Template Injection (Jinja2) targeting Rust (Actix)

**Target Area**: Internal Logic / Rust (Actix) architecture
**Description**: An extensive operational guide for identifying and exploiting Server-Side Template Injection (Jinja2) within a Third-party integration webhook. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Rust (Actix) parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Third-party integration webhook. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on Server-Side Template Injection (Jinja2). Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 5 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Lateral movement to internal databases.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 41. Remote Code Execution - Insecure Deserialization (PHP) targeting GraphQL (Apollo)

**Target Area**: Internal Logic / GraphQL (Apollo) architecture
**Description**: An extensive operational guide for identifying and exploiting Insecure Deserialization (PHP) within a Authenticated endpoint acting as an administrator. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how GraphQL (Apollo) parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Authenticated endpoint acting as an administrator. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on Insecure Deserialization (PHP). Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 5 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Deployment of cryptominers across the backend fleet.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 42. Remote Code Execution - RCE via YAML Parsing Flaws targeting Django (Python)

**Target Area**: Internal Logic / Django (Python) architecture
**Description**: An extensive operational guide for identifying and exploiting RCE via YAML Parsing Flaws within a Authenticated endpoint acting as a standard user. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Django (Python) parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Authenticated endpoint acting as a standard user. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on RCE via YAML Parsing Flaws. Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 3 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Exfiltration of all environment variables and secrets.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 43. Remote Code Execution - Command Injection via eval() targeting Laravel (PHP)

**Target Area**: Internal Logic / Laravel (PHP) architecture
**Description**: An extensive operational guide for identifying and exploiting Command Injection via eval() within a Authenticated endpoint acting as a standard user. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Laravel (PHP) parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Authenticated endpoint acting as a standard user. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on Command Injection via eval(). Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 6 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Lateral movement to internal databases.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 44. Remote Code Execution - Server-Side Template Injection (Jinja2) targeting Symfony (PHP)

**Target Area**: Internal Logic / Symfony (PHP) architecture
**Description**: An extensive operational guide for identifying and exploiting Server-Side Template Injection (Jinja2) within a Multi-tenant application environment. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Symfony (PHP) parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Multi-tenant application environment. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on Server-Side Template Injection (Jinja2). Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 4 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Full system compromise leading to a reverse shell.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 45. Remote Code Execution - RCE via YAML Parsing Flaws targeting Go (Fiber)

**Target Area**: Internal Logic / Go (Fiber) architecture
**Description**: An extensive operational guide for identifying and exploiting RCE via YAML Parsing Flaws within a Beta feature accessible via feature flags. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Go (Fiber) parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Beta feature accessible via feature flags. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on RCE via YAML Parsing Flaws. Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 4 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Exfiltration of all environment variables and secrets.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 46. Remote Code Execution - Command Injection via shell_exec targeting FastAPI

**Target Area**: Internal Logic / FastAPI architecture
**Description**: An extensive operational guide for identifying and exploiting Command Injection via shell_exec within a Third-party integration webhook. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how FastAPI parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Third-party integration webhook. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on Command Injection via shell_exec. Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 6 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Deployment of cryptominers across the backend fleet.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 47. Remote Code Execution - Insecure Deserialization (PHP) targeting Firebase

**Target Area**: Internal Logic / Firebase architecture
**Description**: An extensive operational guide for identifying and exploiting Insecure Deserialization (PHP) within a Authenticated endpoint acting as an administrator. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Firebase parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Authenticated endpoint acting as an administrator. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on Insecure Deserialization (PHP). Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 7 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Deployment of cryptominers across the backend fleet.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 48. Remote Code Execution - Command Injection via shell_exec targeting Spring Boot (Java)

**Target Area**: Internal Logic / Spring Boot (Java) architecture
**Description**: An extensive operational guide for identifying and exploiting Command Injection via shell_exec within a Authenticated endpoint acting as an administrator. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Spring Boot (Java) parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Authenticated endpoint acting as an administrator. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on Command Injection via shell_exec. Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 5 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Full system compromise leading to a reverse shell.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 49. Remote Code Execution - Server-Side Template Injection (Jinja2) targeting Firebase

**Target Area**: Internal Logic / Firebase architecture
**Description**: An extensive operational guide for identifying and exploiting Server-Side Template Injection (Jinja2) within a Authenticated endpoint acting as an administrator. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Firebase parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Authenticated endpoint acting as an administrator. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on Server-Side Template Injection (Jinja2). Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 4 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Full system compromise leading to a reverse shell.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 50. Remote Code Execution - Insecure Deserialization (PHP) targeting Rust (Actix)

**Target Area**: Internal Logic / Rust (Actix) architecture
**Description**: An extensive operational guide for identifying and exploiting Insecure Deserialization (PHP) within a Mobile application backend API. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Rust (Actix) parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Mobile application backend API. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on Insecure Deserialization (PHP). Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 5 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Deployment of cryptominers across the backend fleet.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 51. Remote Code Execution - Insecure Deserialization (Python Pickle) targeting Spring Boot (Java)

**Target Area**: Internal Logic / Spring Boot (Java) architecture
**Description**: An extensive operational guide for identifying and exploiting Insecure Deserialization (Python Pickle) within a Multi-tenant application environment. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Spring Boot (Java) parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Multi-tenant application environment. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on Insecure Deserialization (Python Pickle). Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 6 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Lateral movement to internal databases.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 52. Remote Code Execution - Command Injection via eval() targeting Apigee

**Target Area**: Internal Logic / Apigee architecture
**Description**: An extensive operational guide for identifying and exploiting Command Injection via eval() within a Authenticated endpoint acting as a standard user. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Apigee parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Authenticated endpoint acting as a standard user. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on Command Injection via eval(). Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 3 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Deployment of cryptominers across the backend fleet.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 53. Remote Code Execution - OS Command Injection via Filenames targeting Laravel (PHP)

**Target Area**: Internal Logic / Laravel (PHP) architecture
**Description**: An extensive operational guide for identifying and exploiting OS Command Injection via Filenames within a Third-party integration webhook. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Laravel (PHP) parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Third-party integration webhook. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on OS Command Injection via Filenames. Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 6 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Exfiltration of all environment variables and secrets.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 54. Remote Code Execution - Server-Side Template Injection (Jinja2) targeting Ruby on Rails

**Target Area**: Internal Logic / Ruby on Rails architecture
**Description**: An extensive operational guide for identifying and exploiting Server-Side Template Injection (Jinja2) within a Multi-tenant application environment. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Ruby on Rails parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Multi-tenant application environment. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on Server-Side Template Injection (Jinja2). Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 6 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Deployment of cryptominers across the backend fleet.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 55. Remote Code Execution - RCE via YAML Parsing Flaws targeting Supabase

**Target Area**: Internal Logic / Supabase architecture
**Description**: An extensive operational guide for identifying and exploiting RCE via YAML Parsing Flaws within a Mobile application backend API. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Supabase parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Mobile application backend API. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on RCE via YAML Parsing Flaws. Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 4 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Exfiltration of all environment variables and secrets.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 56. Remote Code Execution - Remote Code Execution via File Upload targeting Symfony (PHP)

**Target Area**: Internal Logic / Symfony (PHP) architecture
**Description**: An extensive operational guide for identifying and exploiting Remote Code Execution via File Upload within a Mobile application backend API. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Symfony (PHP) parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Mobile application backend API. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on Remote Code Execution via File Upload. Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 4 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Full system compromise leading to a reverse shell.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 57. Remote Code Execution - Server-Side Template Injection (Jinja2) targeting Supabase

**Target Area**: Internal Logic / Supabase architecture
**Description**: An extensive operational guide for identifying and exploiting Server-Side Template Injection (Jinja2) within a Multi-tenant application environment. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Supabase parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Multi-tenant application environment. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on Server-Side Template Injection (Jinja2). Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 7 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Full system compromise leading to a reverse shell.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 58. Remote Code Execution - Command Injection via shell_exec targeting ASP.NET Core

**Target Area**: Internal Logic / ASP.NET Core architecture
**Description**: An extensive operational guide for identifying and exploiting Command Injection via shell_exec within a Unauthenticated endpoint exposed to the public. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how ASP.NET Core parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Unauthenticated endpoint exposed to the public. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on Command Injection via shell_exec. Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 5 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Deployment of cryptominers across the backend fleet.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 59. Remote Code Execution - Server-Side Template Injection (Twig) targeting Node.js/Express

**Target Area**: Internal Logic / Node.js/Express architecture
**Description**: An extensive operational guide for identifying and exploiting Server-Side Template Injection (Twig) within a Authenticated endpoint acting as a standard user. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Node.js/Express parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Authenticated endpoint acting as a standard user. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on Server-Side Template Injection (Twig). Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 4 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Full system compromise leading to a reverse shell.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 60. Remote Code Execution - Command Injection via shell_exec targeting Symfony (PHP)

**Target Area**: Internal Logic / Symfony (PHP) architecture
**Description**: An extensive operational guide for identifying and exploiting Command Injection via shell_exec within a Authenticated endpoint acting as a standard user. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Symfony (PHP) parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Authenticated endpoint acting as a standard user. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on Command Injection via shell_exec. Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 3 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Full system compromise leading to a reverse shell.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 61. Remote Code Execution - Command Injection via eval() targeting Django (Python)

**Target Area**: Internal Logic / Django (Python) architecture
**Description**: An extensive operational guide for identifying and exploiting Command Injection via eval() within a Unauthenticated endpoint exposed to the public. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Django (Python) parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Unauthenticated endpoint exposed to the public. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on Command Injection via eval(). Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 5 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Lateral movement to internal databases.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 62. Remote Code Execution - Server-Side Template Injection (Jinja2) targeting ASP.NET Core

**Target Area**: Internal Logic / ASP.NET Core architecture
**Description**: An extensive operational guide for identifying and exploiting Server-Side Template Injection (Jinja2) within a Legacy API endpoint slated for deprecation. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how ASP.NET Core parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Legacy API endpoint slated for deprecation. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on Server-Side Template Injection (Jinja2). Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 7 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Exfiltration of all environment variables and secrets.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 63. Remote Code Execution - Command Injection via shell_exec targeting GraphQL (Relay)

**Target Area**: Internal Logic / GraphQL (Relay) architecture
**Description**: An extensive operational guide for identifying and exploiting Command Injection via shell_exec within a Authenticated endpoint acting as an administrator. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how GraphQL (Relay) parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Authenticated endpoint acting as an administrator. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on Command Injection via shell_exec. Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 4 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Exfiltration of all environment variables and secrets.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 64. Remote Code Execution - RCE via YAML Parsing Flaws targeting Supabase

**Target Area**: Internal Logic / Supabase architecture
**Description**: An extensive operational guide for identifying and exploiting RCE via YAML Parsing Flaws within a Third-party integration webhook. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Supabase parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Third-party integration webhook. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on RCE via YAML Parsing Flaws. Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 5 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Full system compromise leading to a reverse shell.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 65. Remote Code Execution - Insecure Deserialization (Java) targeting Go (Gin)

**Target Area**: Internal Logic / Go (Gin) architecture
**Description**: An extensive operational guide for identifying and exploiting Insecure Deserialization (Java) within a Mobile application backend API. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Go (Gin) parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Mobile application backend API. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on Insecure Deserialization (Java). Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 4 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Full system compromise leading to a reverse shell.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 66. Remote Code Execution - Insecure Deserialization (Python Pickle) targeting Spring Boot (Java)

**Target Area**: Internal Logic / Spring Boot (Java) architecture
**Description**: An extensive operational guide for identifying and exploiting Insecure Deserialization (Python Pickle) within a Beta feature accessible via feature flags. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Spring Boot (Java) parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Beta feature accessible via feature flags. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on Insecure Deserialization (Python Pickle). Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 4 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Lateral movement to internal databases.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 67. Remote Code Execution - OS Command Injection via Filenames targeting Supabase

**Target Area**: Internal Logic / Supabase architecture
**Description**: An extensive operational guide for identifying and exploiting OS Command Injection via Filenames within a Authenticated endpoint acting as a standard user. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Supabase parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Authenticated endpoint acting as a standard user. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on OS Command Injection via Filenames. Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 7 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Lateral movement to internal databases.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 68. Remote Code Execution - Server-Side Template Injection (Twig) targeting Laravel (PHP)

**Target Area**: Internal Logic / Laravel (PHP) architecture
**Description**: An extensive operational guide for identifying and exploiting Server-Side Template Injection (Twig) within a Authenticated endpoint acting as a standard user. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Laravel (PHP) parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Authenticated endpoint acting as a standard user. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on Server-Side Template Injection (Twig). Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 4 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Full system compromise leading to a reverse shell.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 69. Remote Code Execution - Remote Code Execution via File Upload targeting Flask (Python)

**Target Area**: Internal Logic / Flask (Python) architecture
**Description**: An extensive operational guide for identifying and exploiting Remote Code Execution via File Upload within a Authenticated endpoint acting as an administrator. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Flask (Python) parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Authenticated endpoint acting as an administrator. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on Remote Code Execution via File Upload. Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 4 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Deployment of cryptominers across the backend fleet.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 70. Remote Code Execution - Insecure Deserialization (Java) targeting GraphQL (Relay)

**Target Area**: Internal Logic / GraphQL (Relay) architecture
**Description**: An extensive operational guide for identifying and exploiting Insecure Deserialization (Java) within a Authenticated endpoint acting as a standard user. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how GraphQL (Relay) parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Authenticated endpoint acting as a standard user. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on Insecure Deserialization (Java). Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 3 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Lateral movement to internal databases.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 71. Remote Code Execution - Insecure Deserialization (Python Pickle) targeting Laravel (PHP)

**Target Area**: Internal Logic / Laravel (PHP) architecture
**Description**: An extensive operational guide for identifying and exploiting Insecure Deserialization (Python Pickle) within a Beta feature accessible via feature flags. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Laravel (PHP) parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Beta feature accessible via feature flags. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on Insecure Deserialization (Python Pickle). Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 7 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Full system compromise leading to a reverse shell.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 72. Remote Code Execution - Remote Code Execution via File Upload targeting Apigee

**Target Area**: Internal Logic / Apigee architecture
**Description**: An extensive operational guide for identifying and exploiting Remote Code Execution via File Upload within a Legacy API endpoint slated for deprecation. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Apigee parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Legacy API endpoint slated for deprecation. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on Remote Code Execution via File Upload. Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 7 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Lateral movement to internal databases.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 73. Remote Code Execution - Insecure Deserialization (PHP) targeting AWS API Gateway

**Target Area**: Internal Logic / AWS API Gateway architecture
**Description**: An extensive operational guide for identifying and exploiting Insecure Deserialization (PHP) within a Legacy API endpoint slated for deprecation. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how AWS API Gateway parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Legacy API endpoint slated for deprecation. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on Insecure Deserialization (PHP). Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 7 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Deployment of cryptominers across the backend fleet.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 74. Remote Code Execution - Remote Code Execution via File Upload targeting Azure API Management

**Target Area**: Internal Logic / Azure API Management architecture
**Description**: An extensive operational guide for identifying and exploiting Remote Code Execution via File Upload within a Third-party integration webhook. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Azure API Management parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Third-party integration webhook. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on Remote Code Execution via File Upload. Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 4 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Exfiltration of all environment variables and secrets.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 75. Remote Code Execution - RCE via YAML Parsing Flaws targeting Apigee

**Target Area**: Internal Logic / Apigee architecture
**Description**: An extensive operational guide for identifying and exploiting RCE via YAML Parsing Flaws within a Authenticated endpoint acting as a standard user. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Apigee parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Authenticated endpoint acting as a standard user. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on RCE via YAML Parsing Flaws. Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 3 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Deployment of cryptominers across the backend fleet.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 76. Remote Code Execution - Command Injection via eval() targeting ASP.NET Core

**Target Area**: Internal Logic / ASP.NET Core architecture
**Description**: An extensive operational guide for identifying and exploiting Command Injection via eval() within a Multi-tenant application environment. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how ASP.NET Core parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Multi-tenant application environment. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on Command Injection via eval(). Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 7 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Deployment of cryptominers across the backend fleet.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 77. Remote Code Execution - OS Command Injection via Filenames targeting GraphQL (Relay)

**Target Area**: Internal Logic / GraphQL (Relay) architecture
**Description**: An extensive operational guide for identifying and exploiting OS Command Injection via Filenames within a Mobile application backend API. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how GraphQL (Relay) parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Mobile application backend API. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on OS Command Injection via Filenames. Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 5 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Full system compromise leading to a reverse shell.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 78. Remote Code Execution - Server-Side Template Injection (Twig) targeting ASP.NET Core

**Target Area**: Internal Logic / ASP.NET Core architecture
**Description**: An extensive operational guide for identifying and exploiting Server-Side Template Injection (Twig) within a Third-party integration webhook. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how ASP.NET Core parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Third-party integration webhook. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on Server-Side Template Injection (Twig). Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 3 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Full system compromise leading to a reverse shell.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 79. Remote Code Execution - OS Command Injection via Filenames targeting Laravel (PHP)

**Target Area**: Internal Logic / Laravel (PHP) architecture
**Description**: An extensive operational guide for identifying and exploiting OS Command Injection via Filenames within a Beta feature accessible via feature flags. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Laravel (PHP) parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Beta feature accessible via feature flags. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on OS Command Injection via Filenames. Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 5 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Lateral movement to internal databases.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 80. Remote Code Execution - Insecure Deserialization (Java) targeting Ruby on Rails

**Target Area**: Internal Logic / Ruby on Rails architecture
**Description**: An extensive operational guide for identifying and exploiting Insecure Deserialization (Java) within a Legacy API endpoint slated for deprecation. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Ruby on Rails parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Legacy API endpoint slated for deprecation. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on Insecure Deserialization (Java). Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 7 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Lateral movement to internal databases.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 81. Remote Code Execution - Remote Code Execution via File Upload targeting ASP.NET Core

**Target Area**: Internal Logic / ASP.NET Core architecture
**Description**: An extensive operational guide for identifying and exploiting Remote Code Execution via File Upload within a Third-party integration webhook. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how ASP.NET Core parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Third-party integration webhook. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on Remote Code Execution via File Upload. Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 4 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Lateral movement to internal databases.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 82. Remote Code Execution - Insecure Deserialization (Python Pickle) targeting Go (Fiber)

**Target Area**: Internal Logic / Go (Fiber) architecture
**Description**: An extensive operational guide for identifying and exploiting Insecure Deserialization (Python Pickle) within a Mobile application backend API. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Go (Fiber) parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Mobile application backend API. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on Insecure Deserialization (Python Pickle). Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 7 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Deployment of cryptominers across the backend fleet.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 83. Remote Code Execution - RCE via YAML Parsing Flaws targeting Kong API Gateway

**Target Area**: Internal Logic / Kong API Gateway architecture
**Description**: An extensive operational guide for identifying and exploiting RCE via YAML Parsing Flaws within a Authenticated endpoint acting as a standard user. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Kong API Gateway parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Authenticated endpoint acting as a standard user. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on RCE via YAML Parsing Flaws. Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 7 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Exfiltration of all environment variables and secrets.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 84. Remote Code Execution - Insecure Deserialization (Java) targeting Firebase

**Target Area**: Internal Logic / Firebase architecture
**Description**: An extensive operational guide for identifying and exploiting Insecure Deserialization (Java) within a Mobile application backend API. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Firebase parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Mobile application backend API. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on Insecure Deserialization (Java). Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 6 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Lateral movement to internal databases.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 85. Remote Code Execution - Insecure Deserialization (Java) targeting Laravel (PHP)

**Target Area**: Internal Logic / Laravel (PHP) architecture
**Description**: An extensive operational guide for identifying and exploiting Insecure Deserialization (Java) within a Mobile application backend API. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Laravel (PHP) parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Mobile application backend API. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on Insecure Deserialization (Java). Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 7 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Full system compromise leading to a reverse shell.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 86. Remote Code Execution - Remote Code Execution via File Upload targeting Go (Fiber)

**Target Area**: Internal Logic / Go (Fiber) architecture
**Description**: An extensive operational guide for identifying and exploiting Remote Code Execution via File Upload within a Unauthenticated endpoint exposed to the public. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Go (Fiber) parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Unauthenticated endpoint exposed to the public. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on Remote Code Execution via File Upload. Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 3 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Lateral movement to internal databases.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 87. Remote Code Execution - Server-Side Template Injection (Twig) targeting Ruby on Rails

**Target Area**: Internal Logic / Ruby on Rails architecture
**Description**: An extensive operational guide for identifying and exploiting Server-Side Template Injection (Twig) within a Beta feature accessible via feature flags. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Ruby on Rails parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Beta feature accessible via feature flags. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on Server-Side Template Injection (Twig). Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 7 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Lateral movement to internal databases.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 88. Remote Code Execution - Command Injection via eval() targeting Ruby on Rails

**Target Area**: Internal Logic / Ruby on Rails architecture
**Description**: An extensive operational guide for identifying and exploiting Command Injection via eval() within a Internal microservice communicating via proxy. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Ruby on Rails parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Internal microservice communicating via proxy. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on Command Injection via eval(). Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 7 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Deployment of cryptominers across the backend fleet.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 89. Remote Code Execution - Server-Side Template Injection (Jinja2) targeting Laravel (PHP)

**Target Area**: Internal Logic / Laravel (PHP) architecture
**Description**: An extensive operational guide for identifying and exploiting Server-Side Template Injection (Jinja2) within a Authenticated endpoint acting as an administrator. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Laravel (PHP) parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Authenticated endpoint acting as an administrator. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on Server-Side Template Injection (Jinja2). Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 7 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Full system compromise leading to a reverse shell.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 90. Remote Code Execution - Command Injection via eval() targeting Firebase

**Target Area**: Internal Logic / Firebase architecture
**Description**: An extensive operational guide for identifying and exploiting Command Injection via eval() within a Authenticated endpoint acting as a standard user. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Firebase parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Authenticated endpoint acting as a standard user. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on Command Injection via eval(). Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 5 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Exfiltration of all environment variables and secrets.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 91. Remote Code Execution - RCE via YAML Parsing Flaws targeting GraphQL (Apollo)

**Target Area**: Internal Logic / GraphQL (Apollo) architecture
**Description**: An extensive operational guide for identifying and exploiting RCE via YAML Parsing Flaws within a Beta feature accessible via feature flags. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how GraphQL (Apollo) parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Beta feature accessible via feature flags. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on RCE via YAML Parsing Flaws. Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 3 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Exfiltration of all environment variables and secrets.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 92. Remote Code Execution - OS Command Injection via Filenames targeting Django (Python)

**Target Area**: Internal Logic / Django (Python) architecture
**Description**: An extensive operational guide for identifying and exploiting OS Command Injection via Filenames within a Legacy API endpoint slated for deprecation. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Django (Python) parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Legacy API endpoint slated for deprecation. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on OS Command Injection via Filenames. Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 4 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Lateral movement to internal databases.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 93. Remote Code Execution - Server-Side Template Injection (Jinja2) targeting FastAPI

**Target Area**: Internal Logic / FastAPI architecture
**Description**: An extensive operational guide for identifying and exploiting Server-Side Template Injection (Jinja2) within a Authenticated endpoint acting as a standard user. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how FastAPI parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Authenticated endpoint acting as a standard user. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on Server-Side Template Injection (Jinja2). Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 3 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Full system compromise leading to a reverse shell.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 94. Remote Code Execution - Insecure Deserialization (Java) targeting Symfony (PHP)

**Target Area**: Internal Logic / Symfony (PHP) architecture
**Description**: An extensive operational guide for identifying and exploiting Insecure Deserialization (Java) within a Mobile application backend API. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Symfony (PHP) parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Mobile application backend API. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on Insecure Deserialization (Java). Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 4 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Deployment of cryptominers across the backend fleet.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 95. Remote Code Execution - OS Command Injection via Filenames targeting Firebase

**Target Area**: Internal Logic / Firebase architecture
**Description**: An extensive operational guide for identifying and exploiting OS Command Injection via Filenames within a Unauthenticated endpoint exposed to the public. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Firebase parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Unauthenticated endpoint exposed to the public. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on OS Command Injection via Filenames. Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 4 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Lateral movement to internal databases.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 96. Remote Code Execution - Server-Side Template Injection (Twig) targeting Supabase

**Target Area**: Internal Logic / Supabase architecture
**Description**: An extensive operational guide for identifying and exploiting Server-Side Template Injection (Twig) within a Mobile application backend API. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Supabase parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Mobile application backend API. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on Server-Side Template Injection (Twig). Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 4 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Full system compromise leading to a reverse shell.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 97. Remote Code Execution - Insecure Deserialization (Python Pickle) targeting Kong API Gateway

**Target Area**: Internal Logic / Kong API Gateway architecture
**Description**: An extensive operational guide for identifying and exploiting Insecure Deserialization (Python Pickle) within a Authenticated endpoint acting as a standard user. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Kong API Gateway parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Authenticated endpoint acting as a standard user. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on Insecure Deserialization (Python Pickle). Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 6 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Full system compromise leading to a reverse shell.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 98. Remote Code Execution - Insecure Deserialization (Java) targeting Azure API Management

**Target Area**: Internal Logic / Azure API Management architecture
**Description**: An extensive operational guide for identifying and exploiting Insecure Deserialization (Java) within a Internal microservice communicating via proxy. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Azure API Management parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Internal microservice communicating via proxy. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on Insecure Deserialization (Java). Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 4 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Deployment of cryptominers across the backend fleet.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 99. Remote Code Execution - OS Command Injection via Filenames targeting GraphQL (Relay)

**Target Area**: Internal Logic / GraphQL (Relay) architecture
**Description**: An extensive operational guide for identifying and exploiting OS Command Injection via Filenames within a Mobile application backend API. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how GraphQL (Relay) parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Mobile application backend API. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on OS Command Injection via Filenames. Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 3 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Full system compromise leading to a reverse shell.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 100. Remote Code Execution - Command Injection via eval() targeting FastAPI

**Target Area**: Internal Logic / FastAPI architecture
**Description**: An extensive operational guide for identifying and exploiting Command Injection via eval() within a Legacy API endpoint slated for deprecation. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how FastAPI parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Legacy API endpoint slated for deprecation. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on Command Injection via eval(). Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 4 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Deployment of cryptominers across the backend fleet.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 101. Remote Code Execution - RCE via YAML Parsing Flaws targeting Django (Python)

**Target Area**: Internal Logic / Django (Python) architecture
**Description**: An extensive operational guide for identifying and exploiting RCE via YAML Parsing Flaws within a Authenticated endpoint acting as a standard user. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Django (Python) parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Authenticated endpoint acting as a standard user. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on RCE via YAML Parsing Flaws. Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 4 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Exfiltration of all environment variables and secrets.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 102. Remote Code Execution - RCE via YAML Parsing Flaws targeting Go (Gin)

**Target Area**: Internal Logic / Go (Gin) architecture
**Description**: An extensive operational guide for identifying and exploiting RCE via YAML Parsing Flaws within a Legacy API endpoint slated for deprecation. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Go (Gin) parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Legacy API endpoint slated for deprecation. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on RCE via YAML Parsing Flaws. Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 7 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Deployment of cryptominers across the backend fleet.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 103. Remote Code Execution - Server-Side Template Injection (Jinja2) targeting Kong API Gateway

**Target Area**: Internal Logic / Kong API Gateway architecture
**Description**: An extensive operational guide for identifying and exploiting Server-Side Template Injection (Jinja2) within a Internal microservice communicating via proxy. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Kong API Gateway parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Internal microservice communicating via proxy. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on Server-Side Template Injection (Jinja2). Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 4 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Lateral movement to internal databases.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 104. Remote Code Execution - Remote Code Execution via File Upload targeting Ruby on Rails

**Target Area**: Internal Logic / Ruby on Rails architecture
**Description**: An extensive operational guide for identifying and exploiting Remote Code Execution via File Upload within a Multi-tenant application environment. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Ruby on Rails parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Multi-tenant application environment. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on Remote Code Execution via File Upload. Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 6 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Deployment of cryptominers across the backend fleet.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 105. Remote Code Execution - Remote Code Execution via File Upload targeting Firebase

**Target Area**: Internal Logic / Firebase architecture
**Description**: An extensive operational guide for identifying and exploiting Remote Code Execution via File Upload within a Mobile application backend API. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Firebase parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Mobile application backend API. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on Remote Code Execution via File Upload. Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 6 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Lateral movement to internal databases.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 106. Remote Code Execution - Server-Side Template Injection (Jinja2) targeting Django (Python)

**Target Area**: Internal Logic / Django (Python) architecture
**Description**: An extensive operational guide for identifying and exploiting Server-Side Template Injection (Jinja2) within a Beta feature accessible via feature flags. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Django (Python) parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Beta feature accessible via feature flags. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on Server-Side Template Injection (Jinja2). Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 4 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Full system compromise leading to a reverse shell.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 107. Remote Code Execution - Remote Code Execution via File Upload targeting AWS API Gateway

**Target Area**: Internal Logic / AWS API Gateway architecture
**Description**: An extensive operational guide for identifying and exploiting Remote Code Execution via File Upload within a Beta feature accessible via feature flags. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how AWS API Gateway parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Beta feature accessible via feature flags. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on Remote Code Execution via File Upload. Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 6 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Full system compromise leading to a reverse shell.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 108. Remote Code Execution - Server-Side Template Injection (Twig) targeting Azure API Management

**Target Area**: Internal Logic / Azure API Management architecture
**Description**: An extensive operational guide for identifying and exploiting Server-Side Template Injection (Twig) within a Unauthenticated endpoint exposed to the public. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Azure API Management parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Unauthenticated endpoint exposed to the public. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on Server-Side Template Injection (Twig). Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 3 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Deployment of cryptominers across the backend fleet.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 109. Remote Code Execution - Remote Code Execution via File Upload targeting Kong API Gateway

**Target Area**: Internal Logic / Kong API Gateway architecture
**Description**: An extensive operational guide for identifying and exploiting Remote Code Execution via File Upload within a Internal microservice communicating via proxy. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Kong API Gateway parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Internal microservice communicating via proxy. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on Remote Code Execution via File Upload. Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 3 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Exfiltration of all environment variables and secrets.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 110. Remote Code Execution - Insecure Deserialization (Python Pickle) targeting Symfony (PHP)

**Target Area**: Internal Logic / Symfony (PHP) architecture
**Description**: An extensive operational guide for identifying and exploiting Insecure Deserialization (Python Pickle) within a Authenticated endpoint acting as a standard user. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Symfony (PHP) parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Authenticated endpoint acting as a standard user. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on Insecure Deserialization (Python Pickle). Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 7 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Deployment of cryptominers across the backend fleet.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 111. Remote Code Execution - Server-Side Template Injection (Twig) targeting ASP.NET Core

**Target Area**: Internal Logic / ASP.NET Core architecture
**Description**: An extensive operational guide for identifying and exploiting Server-Side Template Injection (Twig) within a Authenticated endpoint acting as an administrator. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how ASP.NET Core parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Authenticated endpoint acting as an administrator. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on Server-Side Template Injection (Twig). Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 5 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Deployment of cryptominers across the backend fleet.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 112. Remote Code Execution - Server-Side Template Injection (Twig) targeting Rust (Actix)

**Target Area**: Internal Logic / Rust (Actix) architecture
**Description**: An extensive operational guide for identifying and exploiting Server-Side Template Injection (Twig) within a Authenticated endpoint acting as a standard user. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Rust (Actix) parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Authenticated endpoint acting as a standard user. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on Server-Side Template Injection (Twig). Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 3 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Exfiltration of all environment variables and secrets.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 113. Remote Code Execution - Insecure Deserialization (Python Pickle) targeting GraphQL (Relay)

**Target Area**: Internal Logic / GraphQL (Relay) architecture
**Description**: An extensive operational guide for identifying and exploiting Insecure Deserialization (Python Pickle) within a Beta feature accessible via feature flags. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how GraphQL (Relay) parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Beta feature accessible via feature flags. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on Insecure Deserialization (Python Pickle). Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 4 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Exfiltration of all environment variables and secrets.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 114. Remote Code Execution - Command Injection via shell_exec targeting FastAPI

**Target Area**: Internal Logic / FastAPI architecture
**Description**: An extensive operational guide for identifying and exploiting Command Injection via shell_exec within a Beta feature accessible via feature flags. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how FastAPI parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Beta feature accessible via feature flags. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on Command Injection via shell_exec. Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 4 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Lateral movement to internal databases.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 115. Remote Code Execution - Insecure Deserialization (PHP) targeting Rust (Actix)

**Target Area**: Internal Logic / Rust (Actix) architecture
**Description**: An extensive operational guide for identifying and exploiting Insecure Deserialization (PHP) within a Authenticated endpoint acting as a standard user. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Rust (Actix) parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Authenticated endpoint acting as a standard user. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on Insecure Deserialization (PHP). Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 6 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Deployment of cryptominers across the backend fleet.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 116. Remote Code Execution - Insecure Deserialization (PHP) targeting Supabase

**Target Area**: Internal Logic / Supabase architecture
**Description**: An extensive operational guide for identifying and exploiting Insecure Deserialization (PHP) within a Third-party integration webhook. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Supabase parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Third-party integration webhook. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on Insecure Deserialization (PHP). Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 4 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Exfiltration of all environment variables and secrets.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 117. Remote Code Execution - Command Injection via shell_exec targeting Django (Python)

**Target Area**: Internal Logic / Django (Python) architecture
**Description**: An extensive operational guide for identifying and exploiting Command Injection via shell_exec within a Beta feature accessible via feature flags. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Django (Python) parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Beta feature accessible via feature flags. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on Command Injection via shell_exec. Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 7 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Full system compromise leading to a reverse shell.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 118. Remote Code Execution - Server-Side Template Injection (Jinja2) targeting AWS API Gateway

**Target Area**: Internal Logic / AWS API Gateway architecture
**Description**: An extensive operational guide for identifying and exploiting Server-Side Template Injection (Jinja2) within a Mobile application backend API. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how AWS API Gateway parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Mobile application backend API. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on Server-Side Template Injection (Jinja2). Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 3 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Exfiltration of all environment variables and secrets.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 119. Remote Code Execution - Command Injection via shell_exec targeting Firebase

**Target Area**: Internal Logic / Firebase architecture
**Description**: An extensive operational guide for identifying and exploiting Command Injection via shell_exec within a Authenticated endpoint acting as an administrator. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Firebase parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Authenticated endpoint acting as an administrator. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on Command Injection via shell_exec. Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 5 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Deployment of cryptominers across the backend fleet.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 120. Remote Code Execution - Server-Side Template Injection (Twig) targeting FastAPI

**Target Area**: Internal Logic / FastAPI architecture
**Description**: An extensive operational guide for identifying and exploiting Server-Side Template Injection (Twig) within a Unauthenticated endpoint exposed to the public. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how FastAPI parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Unauthenticated endpoint exposed to the public. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on Server-Side Template Injection (Twig). Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 5 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Deployment of cryptominers across the backend fleet.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 121. Remote Code Execution - OS Command Injection via Filenames targeting Firebase

**Target Area**: Internal Logic / Firebase architecture
**Description**: An extensive operational guide for identifying and exploiting OS Command Injection via Filenames within a Internal microservice communicating via proxy. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Firebase parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Internal microservice communicating via proxy. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on OS Command Injection via Filenames. Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 4 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Full system compromise leading to a reverse shell.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 122. Remote Code Execution - OS Command Injection via Filenames targeting Django (Python)

**Target Area**: Internal Logic / Django (Python) architecture
**Description**: An extensive operational guide for identifying and exploiting OS Command Injection via Filenames within a Legacy API endpoint slated for deprecation. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Django (Python) parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Legacy API endpoint slated for deprecation. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on OS Command Injection via Filenames. Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 4 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Deployment of cryptominers across the backend fleet.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 123. Remote Code Execution - Insecure Deserialization (Java) targeting Firebase

**Target Area**: Internal Logic / Firebase architecture
**Description**: An extensive operational guide for identifying and exploiting Insecure Deserialization (Java) within a Third-party integration webhook. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Firebase parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Third-party integration webhook. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on Insecure Deserialization (Java). Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 6 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Full system compromise leading to a reverse shell.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 124. Remote Code Execution - RCE via YAML Parsing Flaws targeting Flask (Python)

**Target Area**: Internal Logic / Flask (Python) architecture
**Description**: An extensive operational guide for identifying and exploiting RCE via YAML Parsing Flaws within a Legacy API endpoint slated for deprecation. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Flask (Python) parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Legacy API endpoint slated for deprecation. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on RCE via YAML Parsing Flaws. Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 3 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Deployment of cryptominers across the backend fleet.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 125. Remote Code Execution - RCE via YAML Parsing Flaws targeting Ruby on Rails

**Target Area**: Internal Logic / Ruby on Rails architecture
**Description**: An extensive operational guide for identifying and exploiting RCE via YAML Parsing Flaws within a Internal microservice communicating via proxy. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Ruby on Rails parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Internal microservice communicating via proxy. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on RCE via YAML Parsing Flaws. Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 4 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Deployment of cryptominers across the backend fleet.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 126. Remote Code Execution - Insecure Deserialization (PHP) targeting Ruby on Rails

**Target Area**: Internal Logic / Ruby on Rails architecture
**Description**: An extensive operational guide for identifying and exploiting Insecure Deserialization (PHP) within a Authenticated endpoint acting as an administrator. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Ruby on Rails parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Authenticated endpoint acting as an administrator. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on Insecure Deserialization (PHP). Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 5 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Full system compromise leading to a reverse shell.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 127. Remote Code Execution - Insecure Deserialization (PHP) targeting Rust (Actix)

**Target Area**: Internal Logic / Rust (Actix) architecture
**Description**: An extensive operational guide for identifying and exploiting Insecure Deserialization (PHP) within a Legacy API endpoint slated for deprecation. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Rust (Actix) parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Legacy API endpoint slated for deprecation. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on Insecure Deserialization (PHP). Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 6 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Deployment of cryptominers across the backend fleet.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 128. Remote Code Execution - Remote Code Execution via File Upload targeting Kong API Gateway

**Target Area**: Internal Logic / Kong API Gateway architecture
**Description**: An extensive operational guide for identifying and exploiting Remote Code Execution via File Upload within a Mobile application backend API. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Kong API Gateway parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Mobile application backend API. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on Remote Code Execution via File Upload. Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 7 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Full system compromise leading to a reverse shell.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 129. Remote Code Execution - Remote Code Execution via File Upload targeting Spring Boot (Java)

**Target Area**: Internal Logic / Spring Boot (Java) architecture
**Description**: An extensive operational guide for identifying and exploiting Remote Code Execution via File Upload within a Third-party integration webhook. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Spring Boot (Java) parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Third-party integration webhook. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on Remote Code Execution via File Upload. Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 4 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Deployment of cryptominers across the backend fleet.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 130. Remote Code Execution - Server-Side Template Injection (Jinja2) targeting Node.js/Express

**Target Area**: Internal Logic / Node.js/Express architecture
**Description**: An extensive operational guide for identifying and exploiting Server-Side Template Injection (Jinja2) within a Unauthenticated endpoint exposed to the public. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Node.js/Express parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Unauthenticated endpoint exposed to the public. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on Server-Side Template Injection (Jinja2). Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 6 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Deployment of cryptominers across the backend fleet.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 131. Remote Code Execution - RCE via YAML Parsing Flaws targeting Node.js/Express

**Target Area**: Internal Logic / Node.js/Express architecture
**Description**: An extensive operational guide for identifying and exploiting RCE via YAML Parsing Flaws within a Third-party integration webhook. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Node.js/Express parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Third-party integration webhook. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on RCE via YAML Parsing Flaws. Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 6 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Exfiltration of all environment variables and secrets.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 132. Remote Code Execution - Server-Side Template Injection (Jinja2) targeting Symfony (PHP)

**Target Area**: Internal Logic / Symfony (PHP) architecture
**Description**: An extensive operational guide for identifying and exploiting Server-Side Template Injection (Jinja2) within a Authenticated endpoint acting as a standard user. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Symfony (PHP) parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Authenticated endpoint acting as a standard user. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on Server-Side Template Injection (Jinja2). Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 7 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Lateral movement to internal databases.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 133. Remote Code Execution - Insecure Deserialization (Java) targeting Apigee

**Target Area**: Internal Logic / Apigee architecture
**Description**: An extensive operational guide for identifying and exploiting Insecure Deserialization (Java) within a Authenticated endpoint acting as an administrator. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Apigee parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Authenticated endpoint acting as an administrator. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on Insecure Deserialization (Java). Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 3 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Full system compromise leading to a reverse shell.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 134. Remote Code Execution - RCE via YAML Parsing Flaws targeting Rust (Actix)

**Target Area**: Internal Logic / Rust (Actix) architecture
**Description**: An extensive operational guide for identifying and exploiting RCE via YAML Parsing Flaws within a Internal microservice communicating via proxy. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Rust (Actix) parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Internal microservice communicating via proxy. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on RCE via YAML Parsing Flaws. Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 5 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Full system compromise leading to a reverse shell.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 135. Remote Code Execution - Insecure Deserialization (PHP) targeting Go (Fiber)

**Target Area**: Internal Logic / Go (Fiber) architecture
**Description**: An extensive operational guide for identifying and exploiting Insecure Deserialization (PHP) within a Authenticated endpoint acting as an administrator. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Go (Fiber) parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Authenticated endpoint acting as an administrator. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on Insecure Deserialization (PHP). Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 4 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Full system compromise leading to a reverse shell.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 136. Remote Code Execution - Command Injection via shell_exec targeting Go (Gin)

**Target Area**: Internal Logic / Go (Gin) architecture
**Description**: An extensive operational guide for identifying and exploiting Command Injection via shell_exec within a Authenticated endpoint acting as a standard user. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Go (Gin) parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Authenticated endpoint acting as a standard user. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on Command Injection via shell_exec. Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 7 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Full system compromise leading to a reverse shell.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 137. Remote Code Execution - Server-Side Template Injection (Twig) targeting AWS API Gateway

**Target Area**: Internal Logic / AWS API Gateway architecture
**Description**: An extensive operational guide for identifying and exploiting Server-Side Template Injection (Twig) within a Authenticated endpoint acting as a standard user. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how AWS API Gateway parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Authenticated endpoint acting as a standard user. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on Server-Side Template Injection (Twig). Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 7 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Lateral movement to internal databases.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 138. Remote Code Execution - Command Injection via eval() targeting Supabase

**Target Area**: Internal Logic / Supabase architecture
**Description**: An extensive operational guide for identifying and exploiting Command Injection via eval() within a Multi-tenant application environment. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Supabase parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Multi-tenant application environment. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on Command Injection via eval(). Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 4 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Deployment of cryptominers across the backend fleet.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 139. Remote Code Execution - Server-Side Template Injection (Jinja2) targeting Flask (Python)

**Target Area**: Internal Logic / Flask (Python) architecture
**Description**: An extensive operational guide for identifying and exploiting Server-Side Template Injection (Jinja2) within a Third-party integration webhook. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Flask (Python) parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Third-party integration webhook. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on Server-Side Template Injection (Jinja2). Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 6 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Lateral movement to internal databases.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 140. Remote Code Execution - Command Injection via eval() targeting Laravel (PHP)

**Target Area**: Internal Logic / Laravel (PHP) architecture
**Description**: An extensive operational guide for identifying and exploiting Command Injection via eval() within a Legacy API endpoint slated for deprecation. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Laravel (PHP) parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Legacy API endpoint slated for deprecation. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on Command Injection via eval(). Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 5 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Deployment of cryptominers across the backend fleet.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 141. Remote Code Execution - RCE via YAML Parsing Flaws targeting Kong API Gateway

**Target Area**: Internal Logic / Kong API Gateway architecture
**Description**: An extensive operational guide for identifying and exploiting RCE via YAML Parsing Flaws within a Mobile application backend API. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Kong API Gateway parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Mobile application backend API. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on RCE via YAML Parsing Flaws. Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 3 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Exfiltration of all environment variables and secrets.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 142. Remote Code Execution - OS Command Injection via Filenames targeting Node.js/Express

**Target Area**: Internal Logic / Node.js/Express architecture
**Description**: An extensive operational guide for identifying and exploiting OS Command Injection via Filenames within a Authenticated endpoint acting as an administrator. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Node.js/Express parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Authenticated endpoint acting as an administrator. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on OS Command Injection via Filenames. Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 3 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Full system compromise leading to a reverse shell.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 143. Remote Code Execution - Insecure Deserialization (Python Pickle) targeting Rust (Actix)

**Target Area**: Internal Logic / Rust (Actix) architecture
**Description**: An extensive operational guide for identifying and exploiting Insecure Deserialization (Python Pickle) within a Beta feature accessible via feature flags. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Rust (Actix) parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Beta feature accessible via feature flags. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on Insecure Deserialization (Python Pickle). Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 7 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Exfiltration of all environment variables and secrets.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 144. Remote Code Execution - Insecure Deserialization (Python Pickle) targeting Kong API Gateway

**Target Area**: Internal Logic / Kong API Gateway architecture
**Description**: An extensive operational guide for identifying and exploiting Insecure Deserialization (Python Pickle) within a Internal microservice communicating via proxy. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Kong API Gateway parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Internal microservice communicating via proxy. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on Insecure Deserialization (Python Pickle). Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 4 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Exfiltration of all environment variables and secrets.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 145. Remote Code Execution - Insecure Deserialization (Python Pickle) targeting Firebase

**Target Area**: Internal Logic / Firebase architecture
**Description**: An extensive operational guide for identifying and exploiting Insecure Deserialization (Python Pickle) within a Third-party integration webhook. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Firebase parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Third-party integration webhook. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on Insecure Deserialization (Python Pickle). Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 6 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Lateral movement to internal databases.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 146. Remote Code Execution - Command Injection via eval() targeting Node.js/Express

**Target Area**: Internal Logic / Node.js/Express architecture
**Description**: An extensive operational guide for identifying and exploiting Command Injection via eval() within a Unauthenticated endpoint exposed to the public. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Node.js/Express parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Unauthenticated endpoint exposed to the public. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on Command Injection via eval(). Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 6 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Lateral movement to internal databases.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 147. Remote Code Execution - Insecure Deserialization (PHP) targeting Symfony (PHP)

**Target Area**: Internal Logic / Symfony (PHP) architecture
**Description**: An extensive operational guide for identifying and exploiting Insecure Deserialization (PHP) within a Mobile application backend API. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Symfony (PHP) parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Mobile application backend API. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on Insecure Deserialization (PHP). Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 3 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Lateral movement to internal databases.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 148. Remote Code Execution - Insecure Deserialization (Java) targeting Kong API Gateway

**Target Area**: Internal Logic / Kong API Gateway architecture
**Description**: An extensive operational guide for identifying and exploiting Insecure Deserialization (Java) within a Unauthenticated endpoint exposed to the public. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Kong API Gateway parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Unauthenticated endpoint exposed to the public. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on Insecure Deserialization (Java). Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 5 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Full system compromise leading to a reverse shell.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 149. Remote Code Execution - OS Command Injection via Filenames targeting FastAPI

**Target Area**: Internal Logic / FastAPI architecture
**Description**: An extensive operational guide for identifying and exploiting OS Command Injection via Filenames within a Multi-tenant application environment. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how FastAPI parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Multi-tenant application environment. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on OS Command Injection via Filenames. Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 4 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Deployment of cryptominers across the backend fleet.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 150. Remote Code Execution - Server-Side Template Injection (Twig) targeting ASP.NET Core

**Target Area**: Internal Logic / ASP.NET Core architecture
**Description**: An extensive operational guide for identifying and exploiting Server-Side Template Injection (Twig) within a Multi-tenant application environment. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how ASP.NET Core parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Multi-tenant application environment. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on Server-Side Template Injection (Twig). Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 7 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Full system compromise leading to a reverse shell.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 151. Remote Code Execution - Command Injection via shell_exec targeting Node.js/Express

**Target Area**: Internal Logic / Node.js/Express architecture
**Description**: An extensive operational guide for identifying and exploiting Command Injection via shell_exec within a Third-party integration webhook. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Node.js/Express parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Third-party integration webhook. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on Command Injection via shell_exec. Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 6 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Full system compromise leading to a reverse shell.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 152. Remote Code Execution - Remote Code Execution via File Upload targeting Go (Fiber)

**Target Area**: Internal Logic / Go (Fiber) architecture
**Description**: An extensive operational guide for identifying and exploiting Remote Code Execution via File Upload within a Third-party integration webhook. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Go (Fiber) parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Third-party integration webhook. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on Remote Code Execution via File Upload. Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 7 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Deployment of cryptominers across the backend fleet.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 153. Remote Code Execution - Insecure Deserialization (Python Pickle) targeting GraphQL (Relay)

**Target Area**: Internal Logic / GraphQL (Relay) architecture
**Description**: An extensive operational guide for identifying and exploiting Insecure Deserialization (Python Pickle) within a Multi-tenant application environment. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how GraphQL (Relay) parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Multi-tenant application environment. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on Insecure Deserialization (Python Pickle). Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 4 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Lateral movement to internal databases.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 154. Remote Code Execution - Command Injection via eval() targeting FastAPI

**Target Area**: Internal Logic / FastAPI architecture
**Description**: An extensive operational guide for identifying and exploiting Command Injection via eval() within a Unauthenticated endpoint exposed to the public. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how FastAPI parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Unauthenticated endpoint exposed to the public. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on Command Injection via eval(). Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 3 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Full system compromise leading to a reverse shell.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 155. Remote Code Execution - Command Injection via eval() targeting Apigee

**Target Area**: Internal Logic / Apigee architecture
**Description**: An extensive operational guide for identifying and exploiting Command Injection via eval() within a Unauthenticated endpoint exposed to the public. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Apigee parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Unauthenticated endpoint exposed to the public. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on Command Injection via eval(). Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 4 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Exfiltration of all environment variables and secrets.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 156. Remote Code Execution - Insecure Deserialization (Python Pickle) targeting Azure API Management

**Target Area**: Internal Logic / Azure API Management architecture
**Description**: An extensive operational guide for identifying and exploiting Insecure Deserialization (Python Pickle) within a Authenticated endpoint acting as a standard user. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Azure API Management parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Authenticated endpoint acting as a standard user. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on Insecure Deserialization (Python Pickle). Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 5 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Deployment of cryptominers across the backend fleet.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 157. Remote Code Execution - Remote Code Execution via File Upload targeting Kong API Gateway

**Target Area**: Internal Logic / Kong API Gateway architecture
**Description**: An extensive operational guide for identifying and exploiting Remote Code Execution via File Upload within a Internal microservice communicating via proxy. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Kong API Gateway parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Internal microservice communicating via proxy. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on Remote Code Execution via File Upload. Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 5 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Deployment of cryptominers across the backend fleet.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 158. Remote Code Execution - Server-Side Template Injection (Twig) targeting Laravel (PHP)

**Target Area**: Internal Logic / Laravel (PHP) architecture
**Description**: An extensive operational guide for identifying and exploiting Server-Side Template Injection (Twig) within a Legacy API endpoint slated for deprecation. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Laravel (PHP) parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Legacy API endpoint slated for deprecation. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on Server-Side Template Injection (Twig). Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 6 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Lateral movement to internal databases.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 159. Remote Code Execution - Remote Code Execution via File Upload targeting Go (Fiber)

**Target Area**: Internal Logic / Go (Fiber) architecture
**Description**: An extensive operational guide for identifying and exploiting Remote Code Execution via File Upload within a Third-party integration webhook. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Go (Fiber) parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Third-party integration webhook. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on Remote Code Execution via File Upload. Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 7 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Exfiltration of all environment variables and secrets.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 160. Remote Code Execution - OS Command Injection via Filenames targeting AWS API Gateway

**Target Area**: Internal Logic / AWS API Gateway architecture
**Description**: An extensive operational guide for identifying and exploiting OS Command Injection via Filenames within a Multi-tenant application environment. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how AWS API Gateway parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Multi-tenant application environment. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on OS Command Injection via Filenames. Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 7 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Exfiltration of all environment variables and secrets.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 161. Remote Code Execution - Insecure Deserialization (Java) targeting Rust (Actix)

**Target Area**: Internal Logic / Rust (Actix) architecture
**Description**: An extensive operational guide for identifying and exploiting Insecure Deserialization (Java) within a Multi-tenant application environment. This module is built for rigorous edge-case testing.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Rust (Actix) parses underlying operating system calls or external HTTP requests.</step>
    <step>Analyze the context: Multi-tenant application environment. Determine if input is sanitized at the edge or passed blindly to backend functions.</step>
    <step>Formulate hypotheses focused on Insecure Deserialization (Java). Map out exactly how parameter injection could subvert the intended functionality.</step>
    <step>Generate 5 specific, weaponized payloads designed to bypass common regular expression filters.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Exfiltration of all environment variables and secrets.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 162. Remote Code Execution - OS Command Injection via Filenames targeting Rust (Actix)

**Target Area**: Internal Logic / Rust (Actix) architecture
**Description**: An extensive operational guide for identifying and exploiting OS Command Injection via Filenames within a Third-party integration webhook. This module is built for rigorous edge-case testing and deep analysis.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Rust (Actix) handles user-supplied data in the context of OS Command Injection via Filenames.</step>
    <step>Analyze the context: Third-party integration webhook. Evaluate if the input is directly reflected, evaluated, or passed to an insecure deserializer/parser.</step>
    <step>Formulate hypotheses focused on OS Command Injection via Filenames. Map out exactly how the vulnerability could be triggered, bypassing any apparent frontend validation.</step>
    <step>Generate 7 specific, weaponized payloads designed to exploit the logic flow, utilizing advanced evasion techniques if necessary.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Deployment of cryptominers across the backend fleet.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 163. Remote Code Execution - Remote Code Execution via File Upload targeting Rust (Actix)

**Target Area**: Internal Logic / Rust (Actix) architecture
**Description**: An extensive operational guide for identifying and exploiting Remote Code Execution via File Upload within a Authenticated endpoint acting as a standard user. This module is built for rigorous edge-case testing and deep analysis.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Rust (Actix) handles user-supplied data in the context of Remote Code Execution via File Upload.</step>
    <step>Analyze the context: Authenticated endpoint acting as a standard user. Evaluate if the input is directly reflected, evaluated, or passed to an insecure deserializer/parser.</step>
    <step>Formulate hypotheses focused on Remote Code Execution via File Upload. Map out exactly how the vulnerability could be triggered, bypassing any apparent frontend validation.</step>
    <step>Generate 4 specific, weaponized payloads designed to exploit the logic flow, utilizing advanced evasion techniques if necessary.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Exfiltration of all environment variables and secrets.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 164. Remote Code Execution - Insecure Deserialization (PHP) targeting Node.js/Express

**Target Area**: Internal Logic / Node.js/Express architecture
**Description**: An extensive operational guide for identifying and exploiting Insecure Deserialization (PHP) within a Beta feature accessible via feature flags. This module is built for rigorous edge-case testing and deep analysis.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Node.js/Express handles user-supplied data in the context of Insecure Deserialization (PHP).</step>
    <step>Analyze the context: Beta feature accessible via feature flags. Evaluate if the input is directly reflected, evaluated, or passed to an insecure deserializer/parser.</step>
    <step>Formulate hypotheses focused on Insecure Deserialization (PHP). Map out exactly how the vulnerability could be triggered, bypassing any apparent frontend validation.</step>
    <step>Generate 3 specific, weaponized payloads designed to exploit the logic flow, utilizing advanced evasion techniques if necessary.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Full system compromise leading to a reverse shell.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 165. Remote Code Execution - Server-Side Template Injection (Twig) targeting FastAPI

**Target Area**: Internal Logic / FastAPI architecture
**Description**: An extensive operational guide for identifying and exploiting Server-Side Template Injection (Twig) within a Unauthenticated endpoint exposed to the public. This module is built for rigorous edge-case testing and deep analysis.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how FastAPI handles user-supplied data in the context of Server-Side Template Injection (Twig).</step>
    <step>Analyze the context: Unauthenticated endpoint exposed to the public. Evaluate if the input is directly reflected, evaluated, or passed to an insecure deserializer/parser.</step>
    <step>Formulate hypotheses focused on Server-Side Template Injection (Twig). Map out exactly how the vulnerability could be triggered, bypassing any apparent frontend validation.</step>
    <step>Generate 7 specific, weaponized payloads designed to exploit the logic flow, utilizing advanced evasion techniques if necessary.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Lateral movement to internal databases.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 166. Remote Code Execution - Insecure Deserialization (PHP) targeting AWS API Gateway

**Target Area**: Internal Logic / AWS API Gateway architecture
**Description**: An extensive operational guide for identifying and exploiting Insecure Deserialization (PHP) within a Beta feature accessible via feature flags. This module is built for rigorous edge-case testing and deep analysis.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how AWS API Gateway handles user-supplied data in the context of Insecure Deserialization (PHP).</step>
    <step>Analyze the context: Beta feature accessible via feature flags. Evaluate if the input is directly reflected, evaluated, or passed to an insecure deserializer/parser.</step>
    <step>Formulate hypotheses focused on Insecure Deserialization (PHP). Map out exactly how the vulnerability could be triggered, bypassing any apparent frontend validation.</step>
    <step>Generate 4 specific, weaponized payloads designed to exploit the logic flow, utilizing advanced evasion techniques if necessary.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Lateral movement to internal databases.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 167. Remote Code Execution - Server-Side Template Injection (Jinja2) targeting Supabase

**Target Area**: Internal Logic / Supabase architecture
**Description**: An extensive operational guide for identifying and exploiting Server-Side Template Injection (Jinja2) within a Beta feature accessible via feature flags. This module is built for rigorous edge-case testing and deep analysis.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Supabase handles user-supplied data in the context of Server-Side Template Injection (Jinja2).</step>
    <step>Analyze the context: Beta feature accessible via feature flags. Evaluate if the input is directly reflected, evaluated, or passed to an insecure deserializer/parser.</step>
    <step>Formulate hypotheses focused on Server-Side Template Injection (Jinja2). Map out exactly how the vulnerability could be triggered, bypassing any apparent frontend validation.</step>
    <step>Generate 3 specific, weaponized payloads designed to exploit the logic flow, utilizing advanced evasion techniques if necessary.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Deployment of cryptominers across the backend fleet.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 168. Remote Code Execution - OS Command Injection via Filenames targeting Flask (Python)

**Target Area**: Internal Logic / Flask (Python) architecture
**Description**: An extensive operational guide for identifying and exploiting OS Command Injection via Filenames within a Authenticated endpoint acting as an administrator. This module is built for rigorous edge-case testing and deep analysis.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Flask (Python) handles user-supplied data in the context of OS Command Injection via Filenames.</step>
    <step>Analyze the context: Authenticated endpoint acting as an administrator. Evaluate if the input is directly reflected, evaluated, or passed to an insecure deserializer/parser.</step>
    <step>Formulate hypotheses focused on OS Command Injection via Filenames. Map out exactly how the vulnerability could be triggered, bypassing any apparent frontend validation.</step>
    <step>Generate 4 specific, weaponized payloads designed to exploit the logic flow, utilizing advanced evasion techniques if necessary.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Deployment of cryptominers across the backend fleet.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 169. Remote Code Execution - Server-Side Template Injection (Twig) targeting Go (Gin)

**Target Area**: Internal Logic / Go (Gin) architecture
**Description**: An extensive operational guide for identifying and exploiting Server-Side Template Injection (Twig) within a Unauthenticated endpoint exposed to the public. This module is built for rigorous edge-case testing and deep analysis.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Go (Gin) handles user-supplied data in the context of Server-Side Template Injection (Twig).</step>
    <step>Analyze the context: Unauthenticated endpoint exposed to the public. Evaluate if the input is directly reflected, evaluated, or passed to an insecure deserializer/parser.</step>
    <step>Formulate hypotheses focused on Server-Side Template Injection (Twig). Map out exactly how the vulnerability could be triggered, bypassing any apparent frontend validation.</step>
    <step>Generate 4 specific, weaponized payloads designed to exploit the logic flow, utilizing advanced evasion techniques if necessary.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Lateral movement to internal databases.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 170. Remote Code Execution - OS Command Injection via Filenames targeting Django (Python)

**Target Area**: Internal Logic / Django (Python) architecture
**Description**: An extensive operational guide for identifying and exploiting OS Command Injection via Filenames within a Authenticated endpoint acting as an administrator. This module is built for rigorous edge-case testing and deep analysis.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Django (Python) handles user-supplied data in the context of OS Command Injection via Filenames.</step>
    <step>Analyze the context: Authenticated endpoint acting as an administrator. Evaluate if the input is directly reflected, evaluated, or passed to an insecure deserializer/parser.</step>
    <step>Formulate hypotheses focused on OS Command Injection via Filenames. Map out exactly how the vulnerability could be triggered, bypassing any apparent frontend validation.</step>
    <step>Generate 7 specific, weaponized payloads designed to exploit the logic flow, utilizing advanced evasion techniques if necessary.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Deployment of cryptominers across the backend fleet.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 171. Remote Code Execution - Server-Side Template Injection (Twig) targeting Go (Fiber)

**Target Area**: Internal Logic / Go (Fiber) architecture
**Description**: An extensive operational guide for identifying and exploiting Server-Side Template Injection (Twig) within a Authenticated endpoint acting as an administrator. This module is built for rigorous edge-case testing and deep analysis.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Go (Fiber) handles user-supplied data in the context of Server-Side Template Injection (Twig).</step>
    <step>Analyze the context: Authenticated endpoint acting as an administrator. Evaluate if the input is directly reflected, evaluated, or passed to an insecure deserializer/parser.</step>
    <step>Formulate hypotheses focused on Server-Side Template Injection (Twig). Map out exactly how the vulnerability could be triggered, bypassing any apparent frontend validation.</step>
    <step>Generate 7 specific, weaponized payloads designed to exploit the logic flow, utilizing advanced evasion techniques if necessary.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Deployment of cryptominers across the backend fleet.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 172. Remote Code Execution - Server-Side Template Injection (Jinja2) targeting Node.js/Express

**Target Area**: Internal Logic / Node.js/Express architecture
**Description**: An extensive operational guide for identifying and exploiting Server-Side Template Injection (Jinja2) within a Beta feature accessible via feature flags. This module is built for rigorous edge-case testing and deep analysis.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Node.js/Express handles user-supplied data in the context of Server-Side Template Injection (Jinja2).</step>
    <step>Analyze the context: Beta feature accessible via feature flags. Evaluate if the input is directly reflected, evaluated, or passed to an insecure deserializer/parser.</step>
    <step>Formulate hypotheses focused on Server-Side Template Injection (Jinja2). Map out exactly how the vulnerability could be triggered, bypassing any apparent frontend validation.</step>
    <step>Generate 8 specific, weaponized payloads designed to exploit the logic flow, utilizing advanced evasion techniques if necessary.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Full system compromise leading to a reverse shell.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 173. Remote Code Execution - Server-Side Template Injection (Jinja2) targeting FastAPI

**Target Area**: Internal Logic / FastAPI architecture
**Description**: An extensive operational guide for identifying and exploiting Server-Side Template Injection (Jinja2) within a Multi-tenant application environment. This module is built for rigorous edge-case testing and deep analysis.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how FastAPI handles user-supplied data in the context of Server-Side Template Injection (Jinja2).</step>
    <step>Analyze the context: Multi-tenant application environment. Evaluate if the input is directly reflected, evaluated, or passed to an insecure deserializer/parser.</step>
    <step>Formulate hypotheses focused on Server-Side Template Injection (Jinja2). Map out exactly how the vulnerability could be triggered, bypassing any apparent frontend validation.</step>
    <step>Generate 5 specific, weaponized payloads designed to exploit the logic flow, utilizing advanced evasion techniques if necessary.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Deployment of cryptominers across the backend fleet.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 174. Remote Code Execution - OS Command Injection via Filenames targeting AWS API Gateway

**Target Area**: Internal Logic / AWS API Gateway architecture
**Description**: An extensive operational guide for identifying and exploiting OS Command Injection via Filenames within a Mobile application backend API. This module is built for rigorous edge-case testing and deep analysis.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how AWS API Gateway handles user-supplied data in the context of OS Command Injection via Filenames.</step>
    <step>Analyze the context: Mobile application backend API. Evaluate if the input is directly reflected, evaluated, or passed to an insecure deserializer/parser.</step>
    <step>Formulate hypotheses focused on OS Command Injection via Filenames. Map out exactly how the vulnerability could be triggered, bypassing any apparent frontend validation.</step>
    <step>Generate 6 specific, weaponized payloads designed to exploit the logic flow, utilizing advanced evasion techniques if necessary.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Lateral movement to internal databases.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 175. Remote Code Execution - Command Injection via eval() targeting GraphQL (Apollo)

**Target Area**: Internal Logic / GraphQL (Apollo) architecture
**Description**: An extensive operational guide for identifying and exploiting Command Injection via eval() within a Authenticated endpoint acting as an administrator. This module is built for rigorous edge-case testing and deep analysis.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how GraphQL (Apollo) handles user-supplied data in the context of Command Injection via eval().</step>
    <step>Analyze the context: Authenticated endpoint acting as an administrator. Evaluate if the input is directly reflected, evaluated, or passed to an insecure deserializer/parser.</step>
    <step>Formulate hypotheses focused on Command Injection via eval(). Map out exactly how the vulnerability could be triggered, bypassing any apparent frontend validation.</step>
    <step>Generate 8 specific, weaponized payloads designed to exploit the logic flow, utilizing advanced evasion techniques if necessary.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Lateral movement to internal databases.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 176. Remote Code Execution - Command Injection via eval() targeting FastAPI

**Target Area**: Internal Logic / FastAPI architecture
**Description**: An extensive operational guide for identifying and exploiting Command Injection via eval() within a Authenticated endpoint acting as a standard user. This module is built for rigorous edge-case testing and deep analysis.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how FastAPI handles user-supplied data in the context of Command Injection via eval().</step>
    <step>Analyze the context: Authenticated endpoint acting as a standard user. Evaluate if the input is directly reflected, evaluated, or passed to an insecure deserializer/parser.</step>
    <step>Formulate hypotheses focused on Command Injection via eval(). Map out exactly how the vulnerability could be triggered, bypassing any apparent frontend validation.</step>
    <step>Generate 7 specific, weaponized payloads designed to exploit the logic flow, utilizing advanced evasion techniques if necessary.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Deployment of cryptominers across the backend fleet.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 177. Remote Code Execution - Insecure Deserialization (Python Pickle) targeting AWS API Gateway

**Target Area**: Internal Logic / AWS API Gateway architecture
**Description**: An extensive operational guide for identifying and exploiting Insecure Deserialization (Python Pickle) within a Multi-tenant application environment. This module is built for rigorous edge-case testing and deep analysis.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how AWS API Gateway handles user-supplied data in the context of Insecure Deserialization (Python Pickle).</step>
    <step>Analyze the context: Multi-tenant application environment. Evaluate if the input is directly reflected, evaluated, or passed to an insecure deserializer/parser.</step>
    <step>Formulate hypotheses focused on Insecure Deserialization (Python Pickle). Map out exactly how the vulnerability could be triggered, bypassing any apparent frontend validation.</step>
    <step>Generate 3 specific, weaponized payloads designed to exploit the logic flow, utilizing advanced evasion techniques if necessary.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Exfiltration of all environment variables and secrets.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 178. Remote Code Execution - Insecure Deserialization (Java) targeting Apigee

**Target Area**: Internal Logic / Apigee architecture
**Description**: An extensive operational guide for identifying and exploiting Insecure Deserialization (Java) within a Legacy API endpoint slated for deprecation. This module is built for rigorous edge-case testing and deep analysis.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Apigee handles user-supplied data in the context of Insecure Deserialization (Java).</step>
    <step>Analyze the context: Legacy API endpoint slated for deprecation. Evaluate if the input is directly reflected, evaluated, or passed to an insecure deserializer/parser.</step>
    <step>Formulate hypotheses focused on Insecure Deserialization (Java). Map out exactly how the vulnerability could be triggered, bypassing any apparent frontend validation.</step>
    <step>Generate 3 specific, weaponized payloads designed to exploit the logic flow, utilizing advanced evasion techniques if necessary.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Deployment of cryptominers across the backend fleet.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 179. Remote Code Execution - Server-Side Template Injection (Twig) targeting Node.js/Express

**Target Area**: Internal Logic / Node.js/Express architecture
**Description**: An extensive operational guide for identifying and exploiting Server-Side Template Injection (Twig) within a Unauthenticated endpoint exposed to the public. This module is built for rigorous edge-case testing and deep analysis.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Node.js/Express handles user-supplied data in the context of Server-Side Template Injection (Twig).</step>
    <step>Analyze the context: Unauthenticated endpoint exposed to the public. Evaluate if the input is directly reflected, evaluated, or passed to an insecure deserializer/parser.</step>
    <step>Formulate hypotheses focused on Server-Side Template Injection (Twig). Map out exactly how the vulnerability could be triggered, bypassing any apparent frontend validation.</step>
    <step>Generate 4 specific, weaponized payloads designed to exploit the logic flow, utilizing advanced evasion techniques if necessary.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Deployment of cryptominers across the backend fleet.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 180. Remote Code Execution - OS Command Injection via Filenames targeting FastAPI

**Target Area**: Internal Logic / FastAPI architecture
**Description**: An extensive operational guide for identifying and exploiting OS Command Injection via Filenames within a Unauthenticated endpoint exposed to the public. This module is built for rigorous edge-case testing and deep analysis.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how FastAPI handles user-supplied data in the context of OS Command Injection via Filenames.</step>
    <step>Analyze the context: Unauthenticated endpoint exposed to the public. Evaluate if the input is directly reflected, evaluated, or passed to an insecure deserializer/parser.</step>
    <step>Formulate hypotheses focused on OS Command Injection via Filenames. Map out exactly how the vulnerability could be triggered, bypassing any apparent frontend validation.</step>
    <step>Generate 3 specific, weaponized payloads designed to exploit the logic flow, utilizing advanced evasion techniques if necessary.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Exfiltration of all environment variables and secrets.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 181. Remote Code Execution - Insecure Deserialization (Python Pickle) targeting Apigee

**Target Area**: Internal Logic / Apigee architecture
**Description**: An extensive operational guide for identifying and exploiting Insecure Deserialization (Python Pickle) within a Internal microservice communicating via proxy. This module is built for rigorous edge-case testing and deep analysis.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Apigee handles user-supplied data in the context of Insecure Deserialization (Python Pickle).</step>
    <step>Analyze the context: Internal microservice communicating via proxy. Evaluate if the input is directly reflected, evaluated, or passed to an insecure deserializer/parser.</step>
    <step>Formulate hypotheses focused on Insecure Deserialization (Python Pickle). Map out exactly how the vulnerability could be triggered, bypassing any apparent frontend validation.</step>
    <step>Generate 5 specific, weaponized payloads designed to exploit the logic flow, utilizing advanced evasion techniques if necessary.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Lateral movement to internal databases.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 182. Remote Code Execution - Remote Code Execution via File Upload targeting Rust (Actix)

**Target Area**: Internal Logic / Rust (Actix) architecture
**Description**: An extensive operational guide for identifying and exploiting Remote Code Execution via File Upload within a Multi-tenant application environment. This module is built for rigorous edge-case testing and deep analysis.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Rust (Actix) handles user-supplied data in the context of Remote Code Execution via File Upload.</step>
    <step>Analyze the context: Multi-tenant application environment. Evaluate if the input is directly reflected, evaluated, or passed to an insecure deserializer/parser.</step>
    <step>Formulate hypotheses focused on Remote Code Execution via File Upload. Map out exactly how the vulnerability could be triggered, bypassing any apparent frontend validation.</step>
    <step>Generate 4 specific, weaponized payloads designed to exploit the logic flow, utilizing advanced evasion techniques if necessary.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Lateral movement to internal databases.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 183. Remote Code Execution - Insecure Deserialization (Python Pickle) targeting Go (Gin)

**Target Area**: Internal Logic / Go (Gin) architecture
**Description**: An extensive operational guide for identifying and exploiting Insecure Deserialization (Python Pickle) within a Beta feature accessible via feature flags. This module is built for rigorous edge-case testing and deep analysis.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Go (Gin) handles user-supplied data in the context of Insecure Deserialization (Python Pickle).</step>
    <step>Analyze the context: Beta feature accessible via feature flags. Evaluate if the input is directly reflected, evaluated, or passed to an insecure deserializer/parser.</step>
    <step>Formulate hypotheses focused on Insecure Deserialization (Python Pickle). Map out exactly how the vulnerability could be triggered, bypassing any apparent frontend validation.</step>
    <step>Generate 6 specific, weaponized payloads designed to exploit the logic flow, utilizing advanced evasion techniques if necessary.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Full system compromise leading to a reverse shell.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 184. Remote Code Execution - Insecure Deserialization (Java) targeting Django (Python)

**Target Area**: Internal Logic / Django (Python) architecture
**Description**: An extensive operational guide for identifying and exploiting Insecure Deserialization (Java) within a Mobile application backend API. This module is built for rigorous edge-case testing and deep analysis.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Django (Python) handles user-supplied data in the context of Insecure Deserialization (Java).</step>
    <step>Analyze the context: Mobile application backend API. Evaluate if the input is directly reflected, evaluated, or passed to an insecure deserializer/parser.</step>
    <step>Formulate hypotheses focused on Insecure Deserialization (Java). Map out exactly how the vulnerability could be triggered, bypassing any apparent frontend validation.</step>
    <step>Generate 4 specific, weaponized payloads designed to exploit the logic flow, utilizing advanced evasion techniques if necessary.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Deployment of cryptominers across the backend fleet.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 185. Remote Code Execution - Remote Code Execution via File Upload targeting Node.js/Express

**Target Area**: Internal Logic / Node.js/Express architecture
**Description**: An extensive operational guide for identifying and exploiting Remote Code Execution via File Upload within a Legacy API endpoint slated for deprecation. This module is built for rigorous edge-case testing and deep analysis.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Node.js/Express handles user-supplied data in the context of Remote Code Execution via File Upload.</step>
    <step>Analyze the context: Legacy API endpoint slated for deprecation. Evaluate if the input is directly reflected, evaluated, or passed to an insecure deserializer/parser.</step>
    <step>Formulate hypotheses focused on Remote Code Execution via File Upload. Map out exactly how the vulnerability could be triggered, bypassing any apparent frontend validation.</step>
    <step>Generate 5 specific, weaponized payloads designed to exploit the logic flow, utilizing advanced evasion techniques if necessary.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Exfiltration of all environment variables and secrets.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 186. Remote Code Execution - Server-Side Template Injection (Twig) targeting Azure API Management

**Target Area**: Internal Logic / Azure API Management architecture
**Description**: An extensive operational guide for identifying and exploiting Server-Side Template Injection (Twig) within a Mobile application backend API. This module is built for rigorous edge-case testing and deep analysis.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Azure API Management handles user-supplied data in the context of Server-Side Template Injection (Twig).</step>
    <step>Analyze the context: Mobile application backend API. Evaluate if the input is directly reflected, evaluated, or passed to an insecure deserializer/parser.</step>
    <step>Formulate hypotheses focused on Server-Side Template Injection (Twig). Map out exactly how the vulnerability could be triggered, bypassing any apparent frontend validation.</step>
    <step>Generate 5 specific, weaponized payloads designed to exploit the logic flow, utilizing advanced evasion techniques if necessary.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Exfiltration of all environment variables and secrets.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 187. Remote Code Execution - Insecure Deserialization (PHP) targeting GraphQL (Relay)

**Target Area**: Internal Logic / GraphQL (Relay) architecture
**Description**: An extensive operational guide for identifying and exploiting Insecure Deserialization (PHP) within a Third-party integration webhook. This module is built for rigorous edge-case testing and deep analysis.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how GraphQL (Relay) handles user-supplied data in the context of Insecure Deserialization (PHP).</step>
    <step>Analyze the context: Third-party integration webhook. Evaluate if the input is directly reflected, evaluated, or passed to an insecure deserializer/parser.</step>
    <step>Formulate hypotheses focused on Insecure Deserialization (PHP). Map out exactly how the vulnerability could be triggered, bypassing any apparent frontend validation.</step>
    <step>Generate 8 specific, weaponized payloads designed to exploit the logic flow, utilizing advanced evasion techniques if necessary.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Lateral movement to internal databases.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 188. Remote Code Execution - Command Injection via eval() targeting Spring Boot (Java)

**Target Area**: Internal Logic / Spring Boot (Java) architecture
**Description**: An extensive operational guide for identifying and exploiting Command Injection via eval() within a Authenticated endpoint acting as an administrator. This module is built for rigorous edge-case testing and deep analysis.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Spring Boot (Java) handles user-supplied data in the context of Command Injection via eval().</step>
    <step>Analyze the context: Authenticated endpoint acting as an administrator. Evaluate if the input is directly reflected, evaluated, or passed to an insecure deserializer/parser.</step>
    <step>Formulate hypotheses focused on Command Injection via eval(). Map out exactly how the vulnerability could be triggered, bypassing any apparent frontend validation.</step>
    <step>Generate 8 specific, weaponized payloads designed to exploit the logic flow, utilizing advanced evasion techniques if necessary.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Deployment of cryptominers across the backend fleet.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 189. Remote Code Execution - Insecure Deserialization (Java) targeting Supabase

**Target Area**: Internal Logic / Supabase architecture
**Description**: An extensive operational guide for identifying and exploiting Insecure Deserialization (Java) within a Internal microservice communicating via proxy. This module is built for rigorous edge-case testing and deep analysis.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Supabase handles user-supplied data in the context of Insecure Deserialization (Java).</step>
    <step>Analyze the context: Internal microservice communicating via proxy. Evaluate if the input is directly reflected, evaluated, or passed to an insecure deserializer/parser.</step>
    <step>Formulate hypotheses focused on Insecure Deserialization (Java). Map out exactly how the vulnerability could be triggered, bypassing any apparent frontend validation.</step>
    <step>Generate 6 specific, weaponized payloads designed to exploit the logic flow, utilizing advanced evasion techniques if necessary.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Lateral movement to internal databases.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 190. Remote Code Execution - OS Command Injection via Filenames targeting Apigee

**Target Area**: Internal Logic / Apigee architecture
**Description**: An extensive operational guide for identifying and exploiting OS Command Injection via Filenames within a Mobile application backend API. This module is built for rigorous edge-case testing and deep analysis.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Apigee handles user-supplied data in the context of OS Command Injection via Filenames.</step>
    <step>Analyze the context: Mobile application backend API. Evaluate if the input is directly reflected, evaluated, or passed to an insecure deserializer/parser.</step>
    <step>Formulate hypotheses focused on OS Command Injection via Filenames. Map out exactly how the vulnerability could be triggered, bypassing any apparent frontend validation.</step>
    <step>Generate 6 specific, weaponized payloads designed to exploit the logic flow, utilizing advanced evasion techniques if necessary.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Deployment of cryptominers across the backend fleet.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 191. Remote Code Execution - Command Injection via shell_exec targeting Azure API Management

**Target Area**: Internal Logic / Azure API Management architecture
**Description**: An extensive operational guide for identifying and exploiting Command Injection via shell_exec within a Multi-tenant application environment. This module is built for rigorous edge-case testing and deep analysis.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Azure API Management handles user-supplied data in the context of Command Injection via shell_exec.</step>
    <step>Analyze the context: Multi-tenant application environment. Evaluate if the input is directly reflected, evaluated, or passed to an insecure deserializer/parser.</step>
    <step>Formulate hypotheses focused on Command Injection via shell_exec. Map out exactly how the vulnerability could be triggered, bypassing any apparent frontend validation.</step>
    <step>Generate 3 specific, weaponized payloads designed to exploit the logic flow, utilizing advanced evasion techniques if necessary.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Full system compromise leading to a reverse shell.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 192. Remote Code Execution - Insecure Deserialization (PHP) targeting GraphQL (Apollo)

**Target Area**: Internal Logic / GraphQL (Apollo) architecture
**Description**: An extensive operational guide for identifying and exploiting Insecure Deserialization (PHP) within a Mobile application backend API. This module is built for rigorous edge-case testing and deep analysis.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how GraphQL (Apollo) handles user-supplied data in the context of Insecure Deserialization (PHP).</step>
    <step>Analyze the context: Mobile application backend API. Evaluate if the input is directly reflected, evaluated, or passed to an insecure deserializer/parser.</step>
    <step>Formulate hypotheses focused on Insecure Deserialization (PHP). Map out exactly how the vulnerability could be triggered, bypassing any apparent frontend validation.</step>
    <step>Generate 6 specific, weaponized payloads designed to exploit the logic flow, utilizing advanced evasion techniques if necessary.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Lateral movement to internal databases.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 193. Remote Code Execution - Command Injection via shell_exec targeting Laravel (PHP)

**Target Area**: Internal Logic / Laravel (PHP) architecture
**Description**: An extensive operational guide for identifying and exploiting Command Injection via shell_exec within a Authenticated endpoint acting as an administrator. This module is built for rigorous edge-case testing and deep analysis.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Laravel (PHP) handles user-supplied data in the context of Command Injection via shell_exec.</step>
    <step>Analyze the context: Authenticated endpoint acting as an administrator. Evaluate if the input is directly reflected, evaluated, or passed to an insecure deserializer/parser.</step>
    <step>Formulate hypotheses focused on Command Injection via shell_exec. Map out exactly how the vulnerability could be triggered, bypassing any apparent frontend validation.</step>
    <step>Generate 4 specific, weaponized payloads designed to exploit the logic flow, utilizing advanced evasion techniques if necessary.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Deployment of cryptominers across the backend fleet.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 194. Remote Code Execution - Insecure Deserialization (Python Pickle) targeting Azure API Management

**Target Area**: Internal Logic / Azure API Management architecture
**Description**: An extensive operational guide for identifying and exploiting Insecure Deserialization (Python Pickle) within a Unauthenticated endpoint exposed to the public. This module is built for rigorous edge-case testing and deep analysis.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Azure API Management handles user-supplied data in the context of Insecure Deserialization (Python Pickle).</step>
    <step>Analyze the context: Unauthenticated endpoint exposed to the public. Evaluate if the input is directly reflected, evaluated, or passed to an insecure deserializer/parser.</step>
    <step>Formulate hypotheses focused on Insecure Deserialization (Python Pickle). Map out exactly how the vulnerability could be triggered, bypassing any apparent frontend validation.</step>
    <step>Generate 8 specific, weaponized payloads designed to exploit the logic flow, utilizing advanced evasion techniques if necessary.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Exfiltration of all environment variables and secrets.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 195. Remote Code Execution - Server-Side Template Injection (Jinja2) targeting GraphQL (Apollo)

**Target Area**: Internal Logic / GraphQL (Apollo) architecture
**Description**: An extensive operational guide for identifying and exploiting Server-Side Template Injection (Jinja2) within a Authenticated endpoint acting as a standard user. This module is built for rigorous edge-case testing and deep analysis.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how GraphQL (Apollo) handles user-supplied data in the context of Server-Side Template Injection (Jinja2).</step>
    <step>Analyze the context: Authenticated endpoint acting as a standard user. Evaluate if the input is directly reflected, evaluated, or passed to an insecure deserializer/parser.</step>
    <step>Formulate hypotheses focused on Server-Side Template Injection (Jinja2). Map out exactly how the vulnerability could be triggered, bypassing any apparent frontend validation.</step>
    <step>Generate 7 specific, weaponized payloads designed to exploit the logic flow, utilizing advanced evasion techniques if necessary.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Exfiltration of all environment variables and secrets.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 196. Remote Code Execution - OS Command Injection via Filenames targeting Node.js/Express

**Target Area**: Internal Logic / Node.js/Express architecture
**Description**: An extensive operational guide for identifying and exploiting OS Command Injection via Filenames within a Mobile application backend API. This module is built for rigorous edge-case testing and deep analysis.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Node.js/Express handles user-supplied data in the context of OS Command Injection via Filenames.</step>
    <step>Analyze the context: Mobile application backend API. Evaluate if the input is directly reflected, evaluated, or passed to an insecure deserializer/parser.</step>
    <step>Formulate hypotheses focused on OS Command Injection via Filenames. Map out exactly how the vulnerability could be triggered, bypassing any apparent frontend validation.</step>
    <step>Generate 3 specific, weaponized payloads designed to exploit the logic flow, utilizing advanced evasion techniques if necessary.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Deployment of cryptominers across the backend fleet.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 197. Remote Code Execution - Insecure Deserialization (PHP) targeting Laravel (PHP)

**Target Area**: Internal Logic / Laravel (PHP) architecture
**Description**: An extensive operational guide for identifying and exploiting Insecure Deserialization (PHP) within a Authenticated endpoint acting as a standard user. This module is built for rigorous edge-case testing and deep analysis.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Laravel (PHP) handles user-supplied data in the context of Insecure Deserialization (PHP).</step>
    <step>Analyze the context: Authenticated endpoint acting as a standard user. Evaluate if the input is directly reflected, evaluated, or passed to an insecure deserializer/parser.</step>
    <step>Formulate hypotheses focused on Insecure Deserialization (PHP). Map out exactly how the vulnerability could be triggered, bypassing any apparent frontend validation.</step>
    <step>Generate 3 specific, weaponized payloads designed to exploit the logic flow, utilizing advanced evasion techniques if necessary.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Deployment of cryptominers across the backend fleet.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 198. Remote Code Execution - Insecure Deserialization (PHP) targeting Ruby on Rails

**Target Area**: Internal Logic / Ruby on Rails architecture
**Description**: An extensive operational guide for identifying and exploiting Insecure Deserialization (PHP) within a Authenticated endpoint acting as an administrator. This module is built for rigorous edge-case testing and deep analysis.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Ruby on Rails handles user-supplied data in the context of Insecure Deserialization (PHP).</step>
    <step>Analyze the context: Authenticated endpoint acting as an administrator. Evaluate if the input is directly reflected, evaluated, or passed to an insecure deserializer/parser.</step>
    <step>Formulate hypotheses focused on Insecure Deserialization (PHP). Map out exactly how the vulnerability could be triggered, bypassing any apparent frontend validation.</step>
    <step>Generate 8 specific, weaponized payloads designed to exploit the logic flow, utilizing advanced evasion techniques if necessary.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Exfiltration of all environment variables and secrets.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 199. Remote Code Execution - OS Command Injection via Filenames targeting FastAPI

**Target Area**: Internal Logic / FastAPI architecture
**Description**: An extensive operational guide for identifying and exploiting OS Command Injection via Filenames within a Authenticated endpoint acting as a standard user. This module is built for rigorous edge-case testing and deep analysis.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how FastAPI handles user-supplied data in the context of OS Command Injection via Filenames.</step>
    <step>Analyze the context: Authenticated endpoint acting as a standard user. Evaluate if the input is directly reflected, evaluated, or passed to an insecure deserializer/parser.</step>
    <step>Formulate hypotheses focused on OS Command Injection via Filenames. Map out exactly how the vulnerability could be triggered, bypassing any apparent frontend validation.</step>
    <step>Generate 5 specific, weaponized payloads designed to exploit the logic flow, utilizing advanced evasion techniques if necessary.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Deployment of cryptominers across the backend fleet.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 200. Remote Code Execution - Remote Code Execution via File Upload targeting Go (Gin)

**Target Area**: Internal Logic / Go (Gin) architecture
**Description**: An extensive operational guide for identifying and exploiting Remote Code Execution via File Upload within a Multi-tenant application environment. This module is built for rigorous edge-case testing and deep analysis.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Go (Gin) handles user-supplied data in the context of Remote Code Execution via File Upload.</step>
    <step>Analyze the context: Multi-tenant application environment. Evaluate if the input is directly reflected, evaluated, or passed to an insecure deserializer/parser.</step>
    <step>Formulate hypotheses focused on Remote Code Execution via File Upload. Map out exactly how the vulnerability could be triggered, bypassing any apparent frontend validation.</step>
    <step>Generate 3 specific, weaponized payloads designed to exploit the logic flow, utilizing advanced evasion techniques if necessary.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Exfiltration of all environment variables and secrets.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 201. Remote Code Execution - Remote Code Execution via File Upload targeting Symfony (PHP)

**Target Area**: Internal Logic / Symfony (PHP) architecture
**Description**: An extensive operational guide for identifying and exploiting Remote Code Execution via File Upload within a Unauthenticated endpoint exposed to the public. This module is built for rigorous edge-case testing and deep analysis.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Symfony (PHP) handles user-supplied data in the context of Remote Code Execution via File Upload.</step>
    <step>Analyze the context: Unauthenticated endpoint exposed to the public. Evaluate if the input is directly reflected, evaluated, or passed to an insecure deserializer/parser.</step>
    <step>Formulate hypotheses focused on Remote Code Execution via File Upload. Map out exactly how the vulnerability could be triggered, bypassing any apparent frontend validation.</step>
    <step>Generate 8 specific, weaponized payloads designed to exploit the logic flow, utilizing advanced evasion techniques if necessary.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Lateral movement to internal databases.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 202. Remote Code Execution - Command Injection via eval() targeting Ruby on Rails

**Target Area**: Internal Logic / Ruby on Rails architecture
**Description**: An extensive operational guide for identifying and exploiting Command Injection via eval() within a Multi-tenant application environment. This module is built for rigorous edge-case testing and deep analysis.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Ruby on Rails handles user-supplied data in the context of Command Injection via eval().</step>
    <step>Analyze the context: Multi-tenant application environment. Evaluate if the input is directly reflected, evaluated, or passed to an insecure deserializer/parser.</step>
    <step>Formulate hypotheses focused on Command Injection via eval(). Map out exactly how the vulnerability could be triggered, bypassing any apparent frontend validation.</step>
    <step>Generate 6 specific, weaponized payloads designed to exploit the logic flow, utilizing advanced evasion techniques if necessary.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Deployment of cryptominers across the backend fleet.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 203. Remote Code Execution - Command Injection via shell_exec targeting Kong API Gateway

**Target Area**: Internal Logic / Kong API Gateway architecture
**Description**: An extensive operational guide for identifying and exploiting Command Injection via shell_exec within a Internal microservice communicating via proxy. This module is built for rigorous edge-case testing and deep analysis.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Kong API Gateway handles user-supplied data in the context of Command Injection via shell_exec.</step>
    <step>Analyze the context: Internal microservice communicating via proxy. Evaluate if the input is directly reflected, evaluated, or passed to an insecure deserializer/parser.</step>
    <step>Formulate hypotheses focused on Command Injection via shell_exec. Map out exactly how the vulnerability could be triggered, bypassing any apparent frontend validation.</step>
    <step>Generate 6 specific, weaponized payloads designed to exploit the logic flow, utilizing advanced evasion techniques if necessary.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Full system compromise leading to a reverse shell.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 204. Remote Code Execution - Command Injection via eval() targeting Laravel (PHP)

**Target Area**: Internal Logic / Laravel (PHP) architecture
**Description**: An extensive operational guide for identifying and exploiting Command Injection via eval() within a Internal microservice communicating via proxy. This module is built for rigorous edge-case testing and deep analysis.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Laravel (PHP) handles user-supplied data in the context of Command Injection via eval().</step>
    <step>Analyze the context: Internal microservice communicating via proxy. Evaluate if the input is directly reflected, evaluated, or passed to an insecure deserializer/parser.</step>
    <step>Formulate hypotheses focused on Command Injection via eval(). Map out exactly how the vulnerability could be triggered, bypassing any apparent frontend validation.</step>
    <step>Generate 6 specific, weaponized payloads designed to exploit the logic flow, utilizing advanced evasion techniques if necessary.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Full system compromise leading to a reverse shell.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 205. Remote Code Execution - Remote Code Execution via File Upload targeting Rust (Actix)

**Target Area**: Internal Logic / Rust (Actix) architecture
**Description**: An extensive operational guide for identifying and exploiting Remote Code Execution via File Upload within a Authenticated endpoint acting as a standard user. This module is built for rigorous edge-case testing and deep analysis.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Rust (Actix) handles user-supplied data in the context of Remote Code Execution via File Upload.</step>
    <step>Analyze the context: Authenticated endpoint acting as a standard user. Evaluate if the input is directly reflected, evaluated, or passed to an insecure deserializer/parser.</step>
    <step>Formulate hypotheses focused on Remote Code Execution via File Upload. Map out exactly how the vulnerability could be triggered, bypassing any apparent frontend validation.</step>
    <step>Generate 4 specific, weaponized payloads designed to exploit the logic flow, utilizing advanced evasion techniques if necessary.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Lateral movement to internal databases.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 206. Remote Code Execution - Server-Side Template Injection (Jinja2) targeting Go (Fiber)

**Target Area**: Internal Logic / Go (Fiber) architecture
**Description**: An extensive operational guide for identifying and exploiting Server-Side Template Injection (Jinja2) within a Third-party integration webhook. This module is built for rigorous edge-case testing and deep analysis.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Go (Fiber) handles user-supplied data in the context of Server-Side Template Injection (Jinja2).</step>
    <step>Analyze the context: Third-party integration webhook. Evaluate if the input is directly reflected, evaluated, or passed to an insecure deserializer/parser.</step>
    <step>Formulate hypotheses focused on Server-Side Template Injection (Jinja2). Map out exactly how the vulnerability could be triggered, bypassing any apparent frontend validation.</step>
    <step>Generate 4 specific, weaponized payloads designed to exploit the logic flow, utilizing advanced evasion techniques if necessary.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Deployment of cryptominers across the backend fleet.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 207. Remote Code Execution - Server-Side Template Injection (Jinja2) targeting Go (Fiber)

**Target Area**: Internal Logic / Go (Fiber) architecture
**Description**: An extensive operational guide for identifying and exploiting Server-Side Template Injection (Jinja2) within a Mobile application backend API. This module is built for rigorous edge-case testing and deep analysis.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Go (Fiber) handles user-supplied data in the context of Server-Side Template Injection (Jinja2).</step>
    <step>Analyze the context: Mobile application backend API. Evaluate if the input is directly reflected, evaluated, or passed to an insecure deserializer/parser.</step>
    <step>Formulate hypotheses focused on Server-Side Template Injection (Jinja2). Map out exactly how the vulnerability could be triggered, bypassing any apparent frontend validation.</step>
    <step>Generate 4 specific, weaponized payloads designed to exploit the logic flow, utilizing advanced evasion techniques if necessary.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Full system compromise leading to a reverse shell.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 208. Remote Code Execution - Server-Side Template Injection (Jinja2) targeting Ruby on Rails

**Target Area**: Internal Logic / Ruby on Rails architecture
**Description**: An extensive operational guide for identifying and exploiting Server-Side Template Injection (Jinja2) within a Authenticated endpoint acting as a standard user. This module is built for rigorous edge-case testing and deep analysis.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Ruby on Rails handles user-supplied data in the context of Server-Side Template Injection (Jinja2).</step>
    <step>Analyze the context: Authenticated endpoint acting as a standard user. Evaluate if the input is directly reflected, evaluated, or passed to an insecure deserializer/parser.</step>
    <step>Formulate hypotheses focused on Server-Side Template Injection (Jinja2). Map out exactly how the vulnerability could be triggered, bypassing any apparent frontend validation.</step>
    <step>Generate 8 specific, weaponized payloads designed to exploit the logic flow, utilizing advanced evasion techniques if necessary.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Lateral movement to internal databases.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 209. Remote Code Execution - Insecure Deserialization (Python Pickle) targeting Firebase

**Target Area**: Internal Logic / Firebase architecture
**Description**: An extensive operational guide for identifying and exploiting Insecure Deserialization (Python Pickle) within a Authenticated endpoint acting as an administrator. This module is built for rigorous edge-case testing and deep analysis.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Firebase handles user-supplied data in the context of Insecure Deserialization (Python Pickle).</step>
    <step>Analyze the context: Authenticated endpoint acting as an administrator. Evaluate if the input is directly reflected, evaluated, or passed to an insecure deserializer/parser.</step>
    <step>Formulate hypotheses focused on Insecure Deserialization (Python Pickle). Map out exactly how the vulnerability could be triggered, bypassing any apparent frontend validation.</step>
    <step>Generate 8 specific, weaponized payloads designed to exploit the logic flow, utilizing advanced evasion techniques if necessary.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Deployment of cryptominers across the backend fleet.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 210. Remote Code Execution - Remote Code Execution via File Upload targeting Symfony (PHP)

**Target Area**: Internal Logic / Symfony (PHP) architecture
**Description**: An extensive operational guide for identifying and exploiting Remote Code Execution via File Upload within a Internal microservice communicating via proxy. This module is built for rigorous edge-case testing and deep analysis.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Symfony (PHP) handles user-supplied data in the context of Remote Code Execution via File Upload.</step>
    <step>Analyze the context: Internal microservice communicating via proxy. Evaluate if the input is directly reflected, evaluated, or passed to an insecure deserializer/parser.</step>
    <step>Formulate hypotheses focused on Remote Code Execution via File Upload. Map out exactly how the vulnerability could be triggered, bypassing any apparent frontend validation.</step>
    <step>Generate 4 specific, weaponized payloads designed to exploit the logic flow, utilizing advanced evasion techniques if necessary.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Lateral movement to internal databases.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 211. Remote Code Execution - Server-Side Template Injection (Twig) targeting Go (Fiber)

**Target Area**: Internal Logic / Go (Fiber) architecture
**Description**: An extensive operational guide for identifying and exploiting Server-Side Template Injection (Twig) within a Legacy API endpoint slated for deprecation. This module is built for rigorous edge-case testing and deep analysis.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Go (Fiber) handles user-supplied data in the context of Server-Side Template Injection (Twig).</step>
    <step>Analyze the context: Legacy API endpoint slated for deprecation. Evaluate if the input is directly reflected, evaluated, or passed to an insecure deserializer/parser.</step>
    <step>Formulate hypotheses focused on Server-Side Template Injection (Twig). Map out exactly how the vulnerability could be triggered, bypassing any apparent frontend validation.</step>
    <step>Generate 4 specific, weaponized payloads designed to exploit the logic flow, utilizing advanced evasion techniques if necessary.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Lateral movement to internal databases.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 212. Remote Code Execution - Server-Side Template Injection (Jinja2) targeting GraphQL (Apollo)

**Target Area**: Internal Logic / GraphQL (Apollo) architecture
**Description**: An extensive operational guide for identifying and exploiting Server-Side Template Injection (Jinja2) within a Mobile application backend API. This module is built for rigorous edge-case testing and deep analysis.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how GraphQL (Apollo) handles user-supplied data in the context of Server-Side Template Injection (Jinja2).</step>
    <step>Analyze the context: Mobile application backend API. Evaluate if the input is directly reflected, evaluated, or passed to an insecure deserializer/parser.</step>
    <step>Formulate hypotheses focused on Server-Side Template Injection (Jinja2). Map out exactly how the vulnerability could be triggered, bypassing any apparent frontend validation.</step>
    <step>Generate 8 specific, weaponized payloads designed to exploit the logic flow, utilizing advanced evasion techniques if necessary.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Full system compromise leading to a reverse shell.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 213. Remote Code Execution - RCE via YAML Parsing Flaws targeting AWS API Gateway

**Target Area**: Internal Logic / AWS API Gateway architecture
**Description**: An extensive operational guide for identifying and exploiting RCE via YAML Parsing Flaws within a Legacy API endpoint slated for deprecation. This module is built for rigorous edge-case testing and deep analysis.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how AWS API Gateway handles user-supplied data in the context of RCE via YAML Parsing Flaws.</step>
    <step>Analyze the context: Legacy API endpoint slated for deprecation. Evaluate if the input is directly reflected, evaluated, or passed to an insecure deserializer/parser.</step>
    <step>Formulate hypotheses focused on RCE via YAML Parsing Flaws. Map out exactly how the vulnerability could be triggered, bypassing any apparent frontend validation.</step>
    <step>Generate 5 specific, weaponized payloads designed to exploit the logic flow, utilizing advanced evasion techniques if necessary.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Deployment of cryptominers across the backend fleet.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 214. Remote Code Execution - OS Command Injection via Filenames targeting Rust (Actix)

**Target Area**: Internal Logic / Rust (Actix) architecture
**Description**: An extensive operational guide for identifying and exploiting OS Command Injection via Filenames within a Unauthenticated endpoint exposed to the public. This module is built for rigorous edge-case testing and deep analysis.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Rust (Actix) handles user-supplied data in the context of OS Command Injection via Filenames.</step>
    <step>Analyze the context: Unauthenticated endpoint exposed to the public. Evaluate if the input is directly reflected, evaluated, or passed to an insecure deserializer/parser.</step>
    <step>Formulate hypotheses focused on OS Command Injection via Filenames. Map out exactly how the vulnerability could be triggered, bypassing any apparent frontend validation.</step>
    <step>Generate 8 specific, weaponized payloads designed to exploit the logic flow, utilizing advanced evasion techniques if necessary.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Deployment of cryptominers across the backend fleet.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 215. Remote Code Execution - Server-Side Template Injection (Jinja2) targeting Kong API Gateway

**Target Area**: Internal Logic / Kong API Gateway architecture
**Description**: An extensive operational guide for identifying and exploiting Server-Side Template Injection (Jinja2) within a Authenticated endpoint acting as a standard user. This module is built for rigorous edge-case testing and deep analysis.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Kong API Gateway handles user-supplied data in the context of Server-Side Template Injection (Jinja2).</step>
    <step>Analyze the context: Authenticated endpoint acting as a standard user. Evaluate if the input is directly reflected, evaluated, or passed to an insecure deserializer/parser.</step>
    <step>Formulate hypotheses focused on Server-Side Template Injection (Jinja2). Map out exactly how the vulnerability could be triggered, bypassing any apparent frontend validation.</step>
    <step>Generate 7 specific, weaponized payloads designed to exploit the logic flow, utilizing advanced evasion techniques if necessary.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Full system compromise leading to a reverse shell.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 216. Remote Code Execution - Server-Side Template Injection (Twig) targeting Rust (Actix)

**Target Area**: Internal Logic / Rust (Actix) architecture
**Description**: An extensive operational guide for identifying and exploiting Server-Side Template Injection (Twig) within a Multi-tenant application environment. This module is built for rigorous edge-case testing and deep analysis.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Rust (Actix) handles user-supplied data in the context of Server-Side Template Injection (Twig).</step>
    <step>Analyze the context: Multi-tenant application environment. Evaluate if the input is directly reflected, evaluated, or passed to an insecure deserializer/parser.</step>
    <step>Formulate hypotheses focused on Server-Side Template Injection (Twig). Map out exactly how the vulnerability could be triggered, bypassing any apparent frontend validation.</step>
    <step>Generate 4 specific, weaponized payloads designed to exploit the logic flow, utilizing advanced evasion techniques if necessary.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Deployment of cryptominers across the backend fleet.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 217. Remote Code Execution - Remote Code Execution via File Upload targeting FastAPI

**Target Area**: Internal Logic / FastAPI architecture
**Description**: An extensive operational guide for identifying and exploiting Remote Code Execution via File Upload within a Beta feature accessible via feature flags. This module is built for rigorous edge-case testing and deep analysis.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how FastAPI handles user-supplied data in the context of Remote Code Execution via File Upload.</step>
    <step>Analyze the context: Beta feature accessible via feature flags. Evaluate if the input is directly reflected, evaluated, or passed to an insecure deserializer/parser.</step>
    <step>Formulate hypotheses focused on Remote Code Execution via File Upload. Map out exactly how the vulnerability could be triggered, bypassing any apparent frontend validation.</step>
    <step>Generate 8 specific, weaponized payloads designed to exploit the logic flow, utilizing advanced evasion techniques if necessary.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Exfiltration of all environment variables and secrets.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 218. Remote Code Execution - Insecure Deserialization (Python Pickle) targeting Go (Fiber)

**Target Area**: Internal Logic / Go (Fiber) architecture
**Description**: An extensive operational guide for identifying and exploiting Insecure Deserialization (Python Pickle) within a Unauthenticated endpoint exposed to the public. This module is built for rigorous edge-case testing and deep analysis.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Go (Fiber) handles user-supplied data in the context of Insecure Deserialization (Python Pickle).</step>
    <step>Analyze the context: Unauthenticated endpoint exposed to the public. Evaluate if the input is directly reflected, evaluated, or passed to an insecure deserializer/parser.</step>
    <step>Formulate hypotheses focused on Insecure Deserialization (Python Pickle). Map out exactly how the vulnerability could be triggered, bypassing any apparent frontend validation.</step>
    <step>Generate 6 specific, weaponized payloads designed to exploit the logic flow, utilizing advanced evasion techniques if necessary.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Full system compromise leading to a reverse shell.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 219. Remote Code Execution - Server-Side Template Injection (Jinja2) targeting Laravel (PHP)

**Target Area**: Internal Logic / Laravel (PHP) architecture
**Description**: An extensive operational guide for identifying and exploiting Server-Side Template Injection (Jinja2) within a Unauthenticated endpoint exposed to the public. This module is built for rigorous edge-case testing and deep analysis.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Laravel (PHP) handles user-supplied data in the context of Server-Side Template Injection (Jinja2).</step>
    <step>Analyze the context: Unauthenticated endpoint exposed to the public. Evaluate if the input is directly reflected, evaluated, or passed to an insecure deserializer/parser.</step>
    <step>Formulate hypotheses focused on Server-Side Template Injection (Jinja2). Map out exactly how the vulnerability could be triggered, bypassing any apparent frontend validation.</step>
    <step>Generate 6 specific, weaponized payloads designed to exploit the logic flow, utilizing advanced evasion techniques if necessary.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Deployment of cryptominers across the backend fleet.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 220. Remote Code Execution - OS Command Injection via Filenames targeting Django (Python)

**Target Area**: Internal Logic / Django (Python) architecture
**Description**: An extensive operational guide for identifying and exploiting OS Command Injection via Filenames within a Legacy API endpoint slated for deprecation. This module is built for rigorous edge-case testing and deep analysis.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Django (Python) handles user-supplied data in the context of OS Command Injection via Filenames.</step>
    <step>Analyze the context: Legacy API endpoint slated for deprecation. Evaluate if the input is directly reflected, evaluated, or passed to an insecure deserializer/parser.</step>
    <step>Formulate hypotheses focused on OS Command Injection via Filenames. Map out exactly how the vulnerability could be triggered, bypassing any apparent frontend validation.</step>
    <step>Generate 3 specific, weaponized payloads designed to exploit the logic flow, utilizing advanced evasion techniques if necessary.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Lateral movement to internal databases.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 221. Remote Code Execution - Insecure Deserialization (PHP) targeting Supabase

**Target Area**: Internal Logic / Supabase architecture
**Description**: An extensive operational guide for identifying and exploiting Insecure Deserialization (PHP) within a Internal microservice communicating via proxy. This module is built for rigorous edge-case testing and deep analysis.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Supabase handles user-supplied data in the context of Insecure Deserialization (PHP).</step>
    <step>Analyze the context: Internal microservice communicating via proxy. Evaluate if the input is directly reflected, evaluated, or passed to an insecure deserializer/parser.</step>
    <step>Formulate hypotheses focused on Insecure Deserialization (PHP). Map out exactly how the vulnerability could be triggered, bypassing any apparent frontend validation.</step>
    <step>Generate 8 specific, weaponized payloads designed to exploit the logic flow, utilizing advanced evasion techniques if necessary.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Full system compromise leading to a reverse shell.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 222. Remote Code Execution - Remote Code Execution via File Upload targeting Django (Python)

**Target Area**: Internal Logic / Django (Python) architecture
**Description**: An extensive operational guide for identifying and exploiting Remote Code Execution via File Upload within a Authenticated endpoint acting as a standard user. This module is built for rigorous edge-case testing and deep analysis.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Django (Python) handles user-supplied data in the context of Remote Code Execution via File Upload.</step>
    <step>Analyze the context: Authenticated endpoint acting as a standard user. Evaluate if the input is directly reflected, evaluated, or passed to an insecure deserializer/parser.</step>
    <step>Formulate hypotheses focused on Remote Code Execution via File Upload. Map out exactly how the vulnerability could be triggered, bypassing any apparent frontend validation.</step>
    <step>Generate 3 specific, weaponized payloads designed to exploit the logic flow, utilizing advanced evasion techniques if necessary.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Deployment of cryptominers across the backend fleet.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 223. Remote Code Execution - Insecure Deserialization (Python Pickle) targeting GraphQL (Apollo)

**Target Area**: Internal Logic / GraphQL (Apollo) architecture
**Description**: An extensive operational guide for identifying and exploiting Insecure Deserialization (Python Pickle) within a Mobile application backend API. This module is built for rigorous edge-case testing and deep analysis.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how GraphQL (Apollo) handles user-supplied data in the context of Insecure Deserialization (Python Pickle).</step>
    <step>Analyze the context: Mobile application backend API. Evaluate if the input is directly reflected, evaluated, or passed to an insecure deserializer/parser.</step>
    <step>Formulate hypotheses focused on Insecure Deserialization (Python Pickle). Map out exactly how the vulnerability could be triggered, bypassing any apparent frontend validation.</step>
    <step>Generate 6 specific, weaponized payloads designed to exploit the logic flow, utilizing advanced evasion techniques if necessary.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Full system compromise leading to a reverse shell.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 224. Remote Code Execution - Command Injection via shell_exec targeting Go (Gin)

**Target Area**: Internal Logic / Go (Gin) architecture
**Description**: An extensive operational guide for identifying and exploiting Command Injection via shell_exec within a Multi-tenant application environment. This module is built for rigorous edge-case testing and deep analysis.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Go (Gin) handles user-supplied data in the context of Command Injection via shell_exec.</step>
    <step>Analyze the context: Multi-tenant application environment. Evaluate if the input is directly reflected, evaluated, or passed to an insecure deserializer/parser.</step>
    <step>Formulate hypotheses focused on Command Injection via shell_exec. Map out exactly how the vulnerability could be triggered, bypassing any apparent frontend validation.</step>
    <step>Generate 4 specific, weaponized payloads designed to exploit the logic flow, utilizing advanced evasion techniques if necessary.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Lateral movement to internal databases.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 225. Remote Code Execution - RCE via YAML Parsing Flaws targeting Spring Boot (Java)

**Target Area**: Internal Logic / Spring Boot (Java) architecture
**Description**: An extensive operational guide for identifying and exploiting RCE via YAML Parsing Flaws within a Beta feature accessible via feature flags. This module is built for rigorous edge-case testing and deep analysis.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Spring Boot (Java) handles user-supplied data in the context of RCE via YAML Parsing Flaws.</step>
    <step>Analyze the context: Beta feature accessible via feature flags. Evaluate if the input is directly reflected, evaluated, or passed to an insecure deserializer/parser.</step>
    <step>Formulate hypotheses focused on RCE via YAML Parsing Flaws. Map out exactly how the vulnerability could be triggered, bypassing any apparent frontend validation.</step>
    <step>Generate 8 specific, weaponized payloads designed to exploit the logic flow, utilizing advanced evasion techniques if necessary.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Lateral movement to internal databases.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 226. Remote Code Execution - Server-Side Template Injection (Jinja2) targeting Azure API Management

**Target Area**: Internal Logic / Azure API Management architecture
**Description**: An extensive operational guide for identifying and exploiting Server-Side Template Injection (Jinja2) within a Multi-tenant application environment. This module is built for rigorous edge-case testing and deep analysis.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Azure API Management handles user-supplied data in the context of Server-Side Template Injection (Jinja2).</step>
    <step>Analyze the context: Multi-tenant application environment. Evaluate if the input is directly reflected, evaluated, or passed to an insecure deserializer/parser.</step>
    <step>Formulate hypotheses focused on Server-Side Template Injection (Jinja2). Map out exactly how the vulnerability could be triggered, bypassing any apparent frontend validation.</step>
    <step>Generate 3 specific, weaponized payloads designed to exploit the logic flow, utilizing advanced evasion techniques if necessary.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Deployment of cryptominers across the backend fleet.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 227. Remote Code Execution - OS Command Injection via Filenames targeting GraphQL (Relay)

**Target Area**: Internal Logic / GraphQL (Relay) architecture
**Description**: An extensive operational guide for identifying and exploiting OS Command Injection via Filenames within a Multi-tenant application environment. This module is built for rigorous edge-case testing and deep analysis.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how GraphQL (Relay) handles user-supplied data in the context of OS Command Injection via Filenames.</step>
    <step>Analyze the context: Multi-tenant application environment. Evaluate if the input is directly reflected, evaluated, or passed to an insecure deserializer/parser.</step>
    <step>Formulate hypotheses focused on OS Command Injection via Filenames. Map out exactly how the vulnerability could be triggered, bypassing any apparent frontend validation.</step>
    <step>Generate 4 specific, weaponized payloads designed to exploit the logic flow, utilizing advanced evasion techniques if necessary.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Deployment of cryptominers across the backend fleet.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 228. Remote Code Execution - Command Injection via eval() targeting GraphQL (Apollo)

**Target Area**: Internal Logic / GraphQL (Apollo) architecture
**Description**: An extensive operational guide for identifying and exploiting Command Injection via eval() within a Internal microservice communicating via proxy. This module is built for rigorous edge-case testing and deep analysis.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how GraphQL (Apollo) handles user-supplied data in the context of Command Injection via eval().</step>
    <step>Analyze the context: Internal microservice communicating via proxy. Evaluate if the input is directly reflected, evaluated, or passed to an insecure deserializer/parser.</step>
    <step>Formulate hypotheses focused on Command Injection via eval(). Map out exactly how the vulnerability could be triggered, bypassing any apparent frontend validation.</step>
    <step>Generate 4 specific, weaponized payloads designed to exploit the logic flow, utilizing advanced evasion techniques if necessary.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Full system compromise leading to a reverse shell.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 229. Remote Code Execution - Command Injection via shell_exec targeting Node.js/Express

**Target Area**: Internal Logic / Node.js/Express architecture
**Description**: An extensive operational guide for identifying and exploiting Command Injection via shell_exec within a Authenticated endpoint acting as an administrator. This module is built for rigorous edge-case testing and deep analysis.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Node.js/Express handles user-supplied data in the context of Command Injection via shell_exec.</step>
    <step>Analyze the context: Authenticated endpoint acting as an administrator. Evaluate if the input is directly reflected, evaluated, or passed to an insecure deserializer/parser.</step>
    <step>Formulate hypotheses focused on Command Injection via shell_exec. Map out exactly how the vulnerability could be triggered, bypassing any apparent frontend validation.</step>
    <step>Generate 5 specific, weaponized payloads designed to exploit the logic flow, utilizing advanced evasion techniques if necessary.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Lateral movement to internal databases.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 230. Remote Code Execution - Command Injection via shell_exec targeting GraphQL (Relay)

**Target Area**: Internal Logic / GraphQL (Relay) architecture
**Description**: An extensive operational guide for identifying and exploiting Command Injection via shell_exec within a Third-party integration webhook. This module is built for rigorous edge-case testing and deep analysis.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how GraphQL (Relay) handles user-supplied data in the context of Command Injection via shell_exec.</step>
    <step>Analyze the context: Third-party integration webhook. Evaluate if the input is directly reflected, evaluated, or passed to an insecure deserializer/parser.</step>
    <step>Formulate hypotheses focused on Command Injection via shell_exec. Map out exactly how the vulnerability could be triggered, bypassing any apparent frontend validation.</step>
    <step>Generate 5 specific, weaponized payloads designed to exploit the logic flow, utilizing advanced evasion techniques if necessary.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Full system compromise leading to a reverse shell.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 231. Remote Code Execution - Insecure Deserialization (Java) targeting Azure API Management

**Target Area**: Internal Logic / Azure API Management architecture
**Description**: An extensive operational guide for identifying and exploiting Insecure Deserialization (Java) within a Mobile application backend API. This module is built for rigorous edge-case testing and deep analysis.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Azure API Management handles user-supplied data in the context of Insecure Deserialization (Java).</step>
    <step>Analyze the context: Mobile application backend API. Evaluate if the input is directly reflected, evaluated, or passed to an insecure deserializer/parser.</step>
    <step>Formulate hypotheses focused on Insecure Deserialization (Java). Map out exactly how the vulnerability could be triggered, bypassing any apparent frontend validation.</step>
    <step>Generate 5 specific, weaponized payloads designed to exploit the logic flow, utilizing advanced evasion techniques if necessary.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Lateral movement to internal databases.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 232. Remote Code Execution - Remote Code Execution via File Upload targeting Spring Boot (Java)

**Target Area**: Internal Logic / Spring Boot (Java) architecture
**Description**: An extensive operational guide for identifying and exploiting Remote Code Execution via File Upload within a Internal microservice communicating via proxy. This module is built for rigorous edge-case testing and deep analysis.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Spring Boot (Java) handles user-supplied data in the context of Remote Code Execution via File Upload.</step>
    <step>Analyze the context: Internal microservice communicating via proxy. Evaluate if the input is directly reflected, evaluated, or passed to an insecure deserializer/parser.</step>
    <step>Formulate hypotheses focused on Remote Code Execution via File Upload. Map out exactly how the vulnerability could be triggered, bypassing any apparent frontend validation.</step>
    <step>Generate 4 specific, weaponized payloads designed to exploit the logic flow, utilizing advanced evasion techniques if necessary.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Deployment of cryptominers across the backend fleet.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 233. Remote Code Execution - Command Injection via shell_exec targeting Supabase

**Target Area**: Internal Logic / Supabase architecture
**Description**: An extensive operational guide for identifying and exploiting Command Injection via shell_exec within a Beta feature accessible via feature flags. This module is built for rigorous edge-case testing and deep analysis.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Supabase handles user-supplied data in the context of Command Injection via shell_exec.</step>
    <step>Analyze the context: Beta feature accessible via feature flags. Evaluate if the input is directly reflected, evaluated, or passed to an insecure deserializer/parser.</step>
    <step>Formulate hypotheses focused on Command Injection via shell_exec. Map out exactly how the vulnerability could be triggered, bypassing any apparent frontend validation.</step>
    <step>Generate 5 specific, weaponized payloads designed to exploit the logic flow, utilizing advanced evasion techniques if necessary.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Full system compromise leading to a reverse shell.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 234. Remote Code Execution - OS Command Injection via Filenames targeting Go (Fiber)

**Target Area**: Internal Logic / Go (Fiber) architecture
**Description**: An extensive operational guide for identifying and exploiting OS Command Injection via Filenames within a Multi-tenant application environment. This module is built for rigorous edge-case testing and deep analysis.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Go (Fiber) handles user-supplied data in the context of OS Command Injection via Filenames.</step>
    <step>Analyze the context: Multi-tenant application environment. Evaluate if the input is directly reflected, evaluated, or passed to an insecure deserializer/parser.</step>
    <step>Formulate hypotheses focused on OS Command Injection via Filenames. Map out exactly how the vulnerability could be triggered, bypassing any apparent frontend validation.</step>
    <step>Generate 5 specific, weaponized payloads designed to exploit the logic flow, utilizing advanced evasion techniques if necessary.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Deployment of cryptominers across the backend fleet.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 235. Remote Code Execution - Remote Code Execution via File Upload targeting FastAPI

**Target Area**: Internal Logic / FastAPI architecture
**Description**: An extensive operational guide for identifying and exploiting Remote Code Execution via File Upload within a Legacy API endpoint slated for deprecation. This module is built for rigorous edge-case testing and deep analysis.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how FastAPI handles user-supplied data in the context of Remote Code Execution via File Upload.</step>
    <step>Analyze the context: Legacy API endpoint slated for deprecation. Evaluate if the input is directly reflected, evaluated, or passed to an insecure deserializer/parser.</step>
    <step>Formulate hypotheses focused on Remote Code Execution via File Upload. Map out exactly how the vulnerability could be triggered, bypassing any apparent frontend validation.</step>
    <step>Generate 5 specific, weaponized payloads designed to exploit the logic flow, utilizing advanced evasion techniques if necessary.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Full system compromise leading to a reverse shell.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 236. Remote Code Execution - Command Injection via shell_exec targeting Symfony (PHP)

**Target Area**: Internal Logic / Symfony (PHP) architecture
**Description**: An extensive operational guide for identifying and exploiting Command Injection via shell_exec within a Legacy API endpoint slated for deprecation. This module is built for rigorous edge-case testing and deep analysis.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Symfony (PHP) handles user-supplied data in the context of Command Injection via shell_exec.</step>
    <step>Analyze the context: Legacy API endpoint slated for deprecation. Evaluate if the input is directly reflected, evaluated, or passed to an insecure deserializer/parser.</step>
    <step>Formulate hypotheses focused on Command Injection via shell_exec. Map out exactly how the vulnerability could be triggered, bypassing any apparent frontend validation.</step>
    <step>Generate 6 specific, weaponized payloads designed to exploit the logic flow, utilizing advanced evasion techniques if necessary.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Exfiltration of all environment variables and secrets.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 237. Remote Code Execution - Server-Side Template Injection (Jinja2) targeting Laravel (PHP)

**Target Area**: Internal Logic / Laravel (PHP) architecture
**Description**: An extensive operational guide for identifying and exploiting Server-Side Template Injection (Jinja2) within a Internal microservice communicating via proxy. This module is built for rigorous edge-case testing and deep analysis.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Laravel (PHP) handles user-supplied data in the context of Server-Side Template Injection (Jinja2).</step>
    <step>Analyze the context: Internal microservice communicating via proxy. Evaluate if the input is directly reflected, evaluated, or passed to an insecure deserializer/parser.</step>
    <step>Formulate hypotheses focused on Server-Side Template Injection (Jinja2). Map out exactly how the vulnerability could be triggered, bypassing any apparent frontend validation.</step>
    <step>Generate 8 specific, weaponized payloads designed to exploit the logic flow, utilizing advanced evasion techniques if necessary.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Full system compromise leading to a reverse shell.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 238. Remote Code Execution - Command Injection via shell_exec targeting Ruby on Rails

**Target Area**: Internal Logic / Ruby on Rails architecture
**Description**: An extensive operational guide for identifying and exploiting Command Injection via shell_exec within a Authenticated endpoint acting as a standard user. This module is built for rigorous edge-case testing and deep analysis.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Ruby on Rails handles user-supplied data in the context of Command Injection via shell_exec.</step>
    <step>Analyze the context: Authenticated endpoint acting as a standard user. Evaluate if the input is directly reflected, evaluated, or passed to an insecure deserializer/parser.</step>
    <step>Formulate hypotheses focused on Command Injection via shell_exec. Map out exactly how the vulnerability could be triggered, bypassing any apparent frontend validation.</step>
    <step>Generate 3 specific, weaponized payloads designed to exploit the logic flow, utilizing advanced evasion techniques if necessary.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Exfiltration of all environment variables and secrets.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 239. Remote Code Execution - RCE via YAML Parsing Flaws targeting Azure API Management

**Target Area**: Internal Logic / Azure API Management architecture
**Description**: An extensive operational guide for identifying and exploiting RCE via YAML Parsing Flaws within a Authenticated endpoint acting as a standard user. This module is built for rigorous edge-case testing and deep analysis.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Azure API Management handles user-supplied data in the context of RCE via YAML Parsing Flaws.</step>
    <step>Analyze the context: Authenticated endpoint acting as a standard user. Evaluate if the input is directly reflected, evaluated, or passed to an insecure deserializer/parser.</step>
    <step>Formulate hypotheses focused on RCE via YAML Parsing Flaws. Map out exactly how the vulnerability could be triggered, bypassing any apparent frontend validation.</step>
    <step>Generate 6 specific, weaponized payloads designed to exploit the logic flow, utilizing advanced evasion techniques if necessary.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Exfiltration of all environment variables and secrets.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 240. Remote Code Execution - Command Injection via shell_exec targeting AWS API Gateway

**Target Area**: Internal Logic / AWS API Gateway architecture
**Description**: An extensive operational guide for identifying and exploiting Command Injection via shell_exec within a Mobile application backend API. This module is built for rigorous edge-case testing and deep analysis.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how AWS API Gateway handles user-supplied data in the context of Command Injection via shell_exec.</step>
    <step>Analyze the context: Mobile application backend API. Evaluate if the input is directly reflected, evaluated, or passed to an insecure deserializer/parser.</step>
    <step>Formulate hypotheses focused on Command Injection via shell_exec. Map out exactly how the vulnerability could be triggered, bypassing any apparent frontend validation.</step>
    <step>Generate 4 specific, weaponized payloads designed to exploit the logic flow, utilizing advanced evasion techniques if necessary.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Exfiltration of all environment variables and secrets.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 241. Remote Code Execution - Insecure Deserialization (Java) targeting GraphQL (Relay)

**Target Area**: Internal Logic / GraphQL (Relay) architecture
**Description**: An extensive operational guide for identifying and exploiting Insecure Deserialization (Java) within a Third-party integration webhook. This module is built for rigorous edge-case testing and deep analysis.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how GraphQL (Relay) handles user-supplied data in the context of Insecure Deserialization (Java).</step>
    <step>Analyze the context: Third-party integration webhook. Evaluate if the input is directly reflected, evaluated, or passed to an insecure deserializer/parser.</step>
    <step>Formulate hypotheses focused on Insecure Deserialization (Java). Map out exactly how the vulnerability could be triggered, bypassing any apparent frontend validation.</step>
    <step>Generate 7 specific, weaponized payloads designed to exploit the logic flow, utilizing advanced evasion techniques if necessary.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Deployment of cryptominers across the backend fleet.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 242. Remote Code Execution - Insecure Deserialization (Python Pickle) targeting Supabase

**Target Area**: Internal Logic / Supabase architecture
**Description**: An extensive operational guide for identifying and exploiting Insecure Deserialization (Python Pickle) within a Internal microservice communicating via proxy. This module is built for rigorous edge-case testing and deep analysis.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Supabase handles user-supplied data in the context of Insecure Deserialization (Python Pickle).</step>
    <step>Analyze the context: Internal microservice communicating via proxy. Evaluate if the input is directly reflected, evaluated, or passed to an insecure deserializer/parser.</step>
    <step>Formulate hypotheses focused on Insecure Deserialization (Python Pickle). Map out exactly how the vulnerability could be triggered, bypassing any apparent frontend validation.</step>
    <step>Generate 5 specific, weaponized payloads designed to exploit the logic flow, utilizing advanced evasion techniques if necessary.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Exfiltration of all environment variables and secrets.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 243. Remote Code Execution - Command Injection via shell_exec targeting Apigee

**Target Area**: Internal Logic / Apigee architecture
**Description**: An extensive operational guide for identifying and exploiting Command Injection via shell_exec within a Authenticated endpoint acting as a standard user. This module is built for rigorous edge-case testing and deep analysis.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Apigee handles user-supplied data in the context of Command Injection via shell_exec.</step>
    <step>Analyze the context: Authenticated endpoint acting as a standard user. Evaluate if the input is directly reflected, evaluated, or passed to an insecure deserializer/parser.</step>
    <step>Formulate hypotheses focused on Command Injection via shell_exec. Map out exactly how the vulnerability could be triggered, bypassing any apparent frontend validation.</step>
    <step>Generate 4 specific, weaponized payloads designed to exploit the logic flow, utilizing advanced evasion techniques if necessary.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Deployment of cryptominers across the backend fleet.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 244. Remote Code Execution - Command Injection via eval() targeting Rust (Actix)

**Target Area**: Internal Logic / Rust (Actix) architecture
**Description**: An extensive operational guide for identifying and exploiting Command Injection via eval() within a Authenticated endpoint acting as a standard user. This module is built for rigorous edge-case testing and deep analysis.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Rust (Actix) handles user-supplied data in the context of Command Injection via eval().</step>
    <step>Analyze the context: Authenticated endpoint acting as a standard user. Evaluate if the input is directly reflected, evaluated, or passed to an insecure deserializer/parser.</step>
    <step>Formulate hypotheses focused on Command Injection via eval(). Map out exactly how the vulnerability could be triggered, bypassing any apparent frontend validation.</step>
    <step>Generate 5 specific, weaponized payloads designed to exploit the logic flow, utilizing advanced evasion techniques if necessary.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Exfiltration of all environment variables and secrets.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 245. Remote Code Execution - Command Injection via eval() targeting Node.js/Express

**Target Area**: Internal Logic / Node.js/Express architecture
**Description**: An extensive operational guide for identifying and exploiting Command Injection via eval() within a Multi-tenant application environment. This module is built for rigorous edge-case testing and deep analysis.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Node.js/Express handles user-supplied data in the context of Command Injection via eval().</step>
    <step>Analyze the context: Multi-tenant application environment. Evaluate if the input is directly reflected, evaluated, or passed to an insecure deserializer/parser.</step>
    <step>Formulate hypotheses focused on Command Injection via eval(). Map out exactly how the vulnerability could be triggered, bypassing any apparent frontend validation.</step>
    <step>Generate 8 specific, weaponized payloads designed to exploit the logic flow, utilizing advanced evasion techniques if necessary.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Full system compromise leading to a reverse shell.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 246. Remote Code Execution - Insecure Deserialization (Python Pickle) targeting Django (Python)

**Target Area**: Internal Logic / Django (Python) architecture
**Description**: An extensive operational guide for identifying and exploiting Insecure Deserialization (Python Pickle) within a Beta feature accessible via feature flags. This module is built for rigorous edge-case testing and deep analysis.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Django (Python) handles user-supplied data in the context of Insecure Deserialization (Python Pickle).</step>
    <step>Analyze the context: Beta feature accessible via feature flags. Evaluate if the input is directly reflected, evaluated, or passed to an insecure deserializer/parser.</step>
    <step>Formulate hypotheses focused on Insecure Deserialization (Python Pickle). Map out exactly how the vulnerability could be triggered, bypassing any apparent frontend validation.</step>
    <step>Generate 5 specific, weaponized payloads designed to exploit the logic flow, utilizing advanced evasion techniques if necessary.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Full system compromise leading to a reverse shell.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 247. Remote Code Execution - Insecure Deserialization (PHP) targeting Node.js/Express

**Target Area**: Internal Logic / Node.js/Express architecture
**Description**: An extensive operational guide for identifying and exploiting Insecure Deserialization (PHP) within a Multi-tenant application environment. This module is built for rigorous edge-case testing and deep analysis.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Node.js/Express handles user-supplied data in the context of Insecure Deserialization (PHP).</step>
    <step>Analyze the context: Multi-tenant application environment. Evaluate if the input is directly reflected, evaluated, or passed to an insecure deserializer/parser.</step>
    <step>Formulate hypotheses focused on Insecure Deserialization (PHP). Map out exactly how the vulnerability could be triggered, bypassing any apparent frontend validation.</step>
    <step>Generate 3 specific, weaponized payloads designed to exploit the logic flow, utilizing advanced evasion techniques if necessary.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Deployment of cryptominers across the backend fleet.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 248. Remote Code Execution - OS Command Injection via Filenames targeting FastAPI

**Target Area**: Internal Logic / FastAPI architecture
**Description**: An extensive operational guide for identifying and exploiting OS Command Injection via Filenames within a Authenticated endpoint acting as a standard user. This module is built for rigorous edge-case testing and deep analysis.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how FastAPI handles user-supplied data in the context of OS Command Injection via Filenames.</step>
    <step>Analyze the context: Authenticated endpoint acting as a standard user. Evaluate if the input is directly reflected, evaluated, or passed to an insecure deserializer/parser.</step>
    <step>Formulate hypotheses focused on OS Command Injection via Filenames. Map out exactly how the vulnerability could be triggered, bypassing any apparent frontend validation.</step>
    <step>Generate 4 specific, weaponized payloads designed to exploit the logic flow, utilizing advanced evasion techniques if necessary.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Exfiltration of all environment variables and secrets.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 249. Remote Code Execution - Server-Side Template Injection (Twig) targeting Supabase

**Target Area**: Internal Logic / Supabase architecture
**Description**: An extensive operational guide for identifying and exploiting Server-Side Template Injection (Twig) within a Beta feature accessible via feature flags. This module is built for rigorous edge-case testing and deep analysis.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Supabase handles user-supplied data in the context of Server-Side Template Injection (Twig).</step>
    <step>Analyze the context: Beta feature accessible via feature flags. Evaluate if the input is directly reflected, evaluated, or passed to an insecure deserializer/parser.</step>
    <step>Formulate hypotheses focused on Server-Side Template Injection (Twig). Map out exactly how the vulnerability could be triggered, bypassing any apparent frontend validation.</step>
    <step>Generate 5 specific, weaponized payloads designed to exploit the logic flow, utilizing advanced evasion techniques if necessary.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Full system compromise leading to a reverse shell.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 250. Remote Code Execution - Insecure Deserialization (Python Pickle) targeting Ruby on Rails

**Target Area**: Internal Logic / Ruby on Rails architecture
**Description**: An extensive operational guide for identifying and exploiting Insecure Deserialization (Python Pickle) within a Legacy API endpoint slated for deprecation. This module is built for rigorous edge-case testing and deep analysis.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Ruby on Rails handles user-supplied data in the context of Insecure Deserialization (Python Pickle).</step>
    <step>Analyze the context: Legacy API endpoint slated for deprecation. Evaluate if the input is directly reflected, evaluated, or passed to an insecure deserializer/parser.</step>
    <step>Formulate hypotheses focused on Insecure Deserialization (Python Pickle). Map out exactly how the vulnerability could be triggered, bypassing any apparent frontend validation.</step>
    <step>Generate 8 specific, weaponized payloads designed to exploit the logic flow, utilizing advanced evasion techniques if necessary.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Exfiltration of all environment variables and secrets.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 251. Remote Code Execution - Insecure Deserialization (Java) targeting Azure API Management

**Target Area**: Internal Logic / Azure API Management architecture
**Description**: An extensive operational guide for identifying and exploiting Insecure Deserialization (Java) within a Unauthenticated endpoint exposed to the public. This module is built for rigorous edge-case testing and deep analysis.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Azure API Management handles user-supplied data in the context of Insecure Deserialization (Java).</step>
    <step>Analyze the context: Unauthenticated endpoint exposed to the public. Evaluate if the input is directly reflected, evaluated, or passed to an insecure deserializer/parser.</step>
    <step>Formulate hypotheses focused on Insecure Deserialization (Java). Map out exactly how the vulnerability could be triggered, bypassing any apparent frontend validation.</step>
    <step>Generate 6 specific, weaponized payloads designed to exploit the logic flow, utilizing advanced evasion techniques if necessary.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Lateral movement to internal databases.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 252. Remote Code Execution - Insecure Deserialization (Java) targeting AWS API Gateway

**Target Area**: Internal Logic / AWS API Gateway architecture
**Description**: An extensive operational guide for identifying and exploiting Insecure Deserialization (Java) within a Authenticated endpoint acting as an administrator. This module is built for rigorous edge-case testing and deep analysis.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how AWS API Gateway handles user-supplied data in the context of Insecure Deserialization (Java).</step>
    <step>Analyze the context: Authenticated endpoint acting as an administrator. Evaluate if the input is directly reflected, evaluated, or passed to an insecure deserializer/parser.</step>
    <step>Formulate hypotheses focused on Insecure Deserialization (Java). Map out exactly how the vulnerability could be triggered, bypassing any apparent frontend validation.</step>
    <step>Generate 5 specific, weaponized payloads designed to exploit the logic flow, utilizing advanced evasion techniques if necessary.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Lateral movement to internal databases.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 253. Remote Code Execution - Server-Side Template Injection (Twig) targeting GraphQL (Relay)

**Target Area**: Internal Logic / GraphQL (Relay) architecture
**Description**: An extensive operational guide for identifying and exploiting Server-Side Template Injection (Twig) within a Authenticated endpoint acting as a standard user. This module is built for rigorous edge-case testing and deep analysis.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how GraphQL (Relay) handles user-supplied data in the context of Server-Side Template Injection (Twig).</step>
    <step>Analyze the context: Authenticated endpoint acting as a standard user. Evaluate if the input is directly reflected, evaluated, or passed to an insecure deserializer/parser.</step>
    <step>Formulate hypotheses focused on Server-Side Template Injection (Twig). Map out exactly how the vulnerability could be triggered, bypassing any apparent frontend validation.</step>
    <step>Generate 5 specific, weaponized payloads designed to exploit the logic flow, utilizing advanced evasion techniques if necessary.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Full system compromise leading to a reverse shell.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 254. Remote Code Execution - Command Injection via eval() targeting Ruby on Rails

**Target Area**: Internal Logic / Ruby on Rails architecture
**Description**: An extensive operational guide for identifying and exploiting Command Injection via eval() within a Unauthenticated endpoint exposed to the public. This module is built for rigorous edge-case testing and deep analysis.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how Ruby on Rails handles user-supplied data in the context of Command Injection via eval().</step>
    <step>Analyze the context: Unauthenticated endpoint exposed to the public. Evaluate if the input is directly reflected, evaluated, or passed to an insecure deserializer/parser.</step>
    <step>Formulate hypotheses focused on Command Injection via eval(). Map out exactly how the vulnerability could be triggered, bypassing any apparent frontend validation.</step>
    <step>Generate 7 specific, weaponized payloads designed to exploit the logic flow, utilizing advanced evasion techniques if necessary.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Deployment of cryptominers across the backend fleet.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

### 255. Remote Code Execution - Command Injection via eval() targeting GraphQL (Apollo)

**Target Area**: Internal Logic / GraphQL (Apollo) architecture
**Description**: An extensive operational guide for identifying and exploiting Command Injection via eval() within a Authenticated endpoint acting as a standard user. This module is built for rigorous edge-case testing and deep analysis.

```xml
<skill_module>
  <task_description>
    Execute the bug bounty analysis based on the overarching description and target area defined above.
  </task_description>
  <execution_steps>
    <step>Deconstruct the input data. Specifically observe how GraphQL (Apollo) handles user-supplied data in the context of Command Injection via eval().</step>
    <step>Analyze the context: Authenticated endpoint acting as a standard user. Evaluate if the input is directly reflected, evaluated, or passed to an insecure deserializer/parser.</step>
    <step>Formulate hypotheses focused on Command Injection via eval(). Map out exactly how the vulnerability could be triggered, bypassing any apparent frontend validation.</step>
    <step>Generate 3 specific, weaponized payloads designed to exploit the logic flow, utilizing advanced evasion techniques if necessary.</step>
    <step>Evaluate the potential business impact. If the exploit is successful, assess if it leads to: Lateral movement to internal databases.</step>
  </execution_steps>
  <input_data>
    [PASTE RAW HTTP REQUEST / RESPONSE / CODE SNIPPET HERE. DO NOT FORMAT.]
  </input_data>
</skill_module>
```

